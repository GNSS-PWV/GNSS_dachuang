> **2026-08-17 更新说明：** 本文是早期阶段快照。关于“一阶段预测 PS/TS/WPS 驱动二阶段新模型”的最终正式结论，请以 `第二阶段/2014_2019严格回放式堆叠部署评估_成果汇总_20260817.md` 为准：2014–2019、110,928 个共同样本、36 站跨年度联合覆盖，`clim_surf_p1` RMSE 0.242729 mm，较 GPT3 降低 29.88%。旧 2017 单年堆叠结果不能与该结果混用。
> ## 🎯 最终状态（2026-08-07）
> - **核心科学内容全部完成**：第一阶段 4 参数 GRU+XGB + 第二阶段 ProfileTransformer（官方 36 站 RMSE 0.061，Optuna 优化后）
> - **第三阶段完成**：四季验证 / 误差订正 / Optuna 优化 / 月尺度格网产品 / 端到端两步法 / 部署模拟
> - **GNSS 泛化验证完成**：IGS 2019 ZTD → PWV（9 中国站），武汉与探空真值对比 RMSE 5.78mm、R2 0.897
> - **代码已全面重构并推至 GitHub**：https://github.com/GNSS-PWV/GNSS_dachuang（phase1/phase2/docs/legacy）
> - 待办：论文/PPT 撰写；广州/临港新测数据（如有）补充泛化；服务器恢复后如需可同步更多产物

---

# 项目当前情况总结与下一阶段交接文档

> 项目：基于深度学习的两步法实时GNSS水汽反演（国家级大学生创新创业训练计划）
> 文档生成：2026-08-05　|　负责人：王乐萱　指导老师：吴志露
> 用途：向团队成员 / 下一阶段负责人完整交代项目现状、已完成工作、遗留问题与下一步计划

---

## 0. 文档速览（TL;DR）

- **第一阶段（已完成）**：GRU + XGBoost 预测 4 个气象参数（Ps / Ts / WPS / Tm），代码在 `第一阶段/`，服务器 329 训练站 + 36 测试站。
- **第二阶段（本次已完成代码与本地验证）**：`第二阶段/` 内实现核心创新——**ProfileTransformer 廓线→PWV 直接映射模型**（不用 Tm）。
  - 全部代码已在本机跑通（数据管线 → 训练 → 评估 → 留一站交叉验证 LOSO）。
  - 本地 10 站 LOSO：**RMSE 0.39 mm**（Saastamoinen 全流程 8.40 mm，用真值 Tm 的线性公式 0.24 mm）。
  - 修复 3 个 bug、新增 LOSO 评估脚本与 scalers 保存、更新 README。
- **下一步（交接重点）**：
  1. ~~把全量数据放到服务器，跑全量训练~~ ✅ **已于 2026-08-05 完成**（335 训练站 / 37 测试站，100 轮，L40 GPU，2h54m）；
  2. ~~看热带/湿润站能否追上"真值 Tm 线性公式"~~ ✅ **已超越**：测试集 RMSE 0.0545 mm vs 真值 Tm 基线 0.257 mm（提升 78.8%），37 站最大误差 0.123 mm；
  3. 第三阶段（2026.06–12）按 PPT 规划做模型优化、全球四季验证、误差分析与订正（详见 §6）。

> 🎉 **重大进展（2026-08-05）**：第二阶段 ProfileTransformer 已在服务器全量数据上完成训练与评估，**完全不用 Tm**，37 个未见测试站 PWV RMSE = **0.0545 mm**（R2=1.0000），
> 相对真值 Tm 线性公式（0.257 mm）提升 **78.8%**，相对传统 Saastamoinen 全流程（6.56 mm）提升约 **99%**。核心创新点得到验证。
> 完整结果见 `第二阶段/服务器全量训练结果与任务总结.md` 与 `第二阶段/result_server_full/`。

---

## 1. 项目概况

| 项目 | 内容 |
|------|------|
| 项目名称 | 基于深度学习的两步法实时GNSS水汽反演 |
| 团队成员 | 王乐萱、钱顾冲、程禹卜、李乾凯而、彭怡焱 |
| 指导老师 | 吴志露 |
| 项目类型 | 国家级大学生创新创业训练计划（国上创升级答辩） |
| 总体目标 | 突破传统 GNSS 水汽反演对加权平均温度 Tm 和表面气压 Ps 的强依赖，用全球探空廓线数据驱动 ZWD→PWV 直接转换 |
| 量化指标 | PWV RMSE < 1.5 mm，精度优于 GPT3 约 25–40% |
| 预期成果 | 全球高精度 PWV 数据驱动转换模型；论文 1 篇；专利/软著 1 项 |

### 技术路线（两步法）
- **步骤一（第一阶段）**：深度学习建模预报加权平均温度、地表温度、水汽压、地表气压；
- **步骤二（第二阶段）**：构建从湿延迟 ZWD 到水汽 PWV 的**直接映射模型**（廓线驱动，摆脱 Tm 线性假设）。

### 三大创新点（摘自 PPT）
1. **摆脱传统假设依赖**：湿延迟→水汽直接映射，摆脱 Tm 线性假设与表面气象参数依赖；
2. **廓线序列深度学习框架**：Transformer 处理变长高度层，捕捉温度倒挂、对流层顶变化等；
3. **全球格网化产品**：数据驱动转换系数格网产品，实现无站区域高精度 PWV 反演。

---

## 2. 目录结构与数据情况

### 2.1 根目录 `D:\gnss水汽反演\`

| 路径/文件 | 说明 |
|-----------|------|
| `第一阶段/` | 第一阶段代码与结果（PS / WPS / Ts_Tm 三个参数组的 GRU+XGB 脚本） |
| `第二阶段/` | 第二阶段代码与结果（本阶段重点，见第 4 节） |
| `PWV传统模型情况/` | 传统 Saastamoinen 模型逐站 PWV 结果（2014–2019 逐年 xlsx） |
| `saastamoinen_pwv.py` | Saastamoinen 模型 Python 版（由 .m 转换并修正 2 个 bug） |
| `eval_saastamoinen.py` | 传统模型批量评估脚本（读探空→算 PWV→对比真值） |
| `run_SaastamonienPWV.m` / `SaastamoinenPWV.m` | 原始 MATLAB 版（含纬度双重转换、温度单位 2 个 bug，Python 版已修正） |
| `前codex情况（对话已损坏，仅保留内容）.txt` | 早期 Codex 会话记录（项目沿革） |
| `ppt内容发布.txt` | 两份 PPT 的整理版情况说明 |
| `情况简要说明.txt` | 一句话背景（模型演进：LSTM→GRU+XGB） |
| `*.pptx / *.doc` | 开题报告、升级答辩 PPT 与文档 |

### 2.2 数据情况

| 数据集 | 内容 | 位置 |
|--------|------|------|
| 本地测试集 | 10 个 IGRA 探空站，2014 年全年，4077 条廓线 | `第一阶段/xg_test/*_met.txt` |
| 服务器全量训练 | 329 训练站 + 36 测试站，2014–2018（据早期会话记录） | 服务器（路径见 `run_train.sh` 的 `DATA_DIR` 占位符） |
| 传统模型结果 | 2014–2019 逐年逐站 `*_met_PWV_result.xlsx` | `PWV传统模型情况/2014..2019/` |
| `第一阶段/xg_r/` | 当前为空（可能是早期原始数据目录，已迁移） | — |

**探空文件格式**（`*_met.txt`，每行一个高度层，同一时间戳多行构成垂直廓线，ELV 递增）：
`TIME, YEAR, DOY, LAT, LON, ELV, TS, PS, WPS, ZWD, ZHD, ZTD, PWV, Tm`
- 地表层 = 最低 ELV 行；其 ZWD 即 GNSS 可观测湿延迟，PWV 为真值标签；
- 每个时间戳内 Tm 不变（整层积分量），PWV/ZWD ≈ 转换系数 Pi（约 0.15–0.17）。

### 2.3 运行环境（本机已验证）

- Anaconda 环境：`torch_gpu`（路径 `D:\Users\lenovo\anaconda3\envs\torch_gpu\python.exe`）
- torch 2.7.0+cu126，CUDA 可用；numpy / pandas / scikit-learn / matplotlib 齐备
- 注意：本机导入 torch 需要 `KMP_DUPLICATE_LIB_OK=TRUE`（OpenMP 冲突），代码内已设置

---

## 3. 第一阶段总结（已完成）

### 3.1 目标
用 **GRU + XGBoost 残差修正** 分别预测 4 个气象参数：Ps（地表气压）、WPS（水汽压）、Ts（地表温度）、Tm（加权平均温度）。

### 3.2 代码结构（`第一阶段/`）

| 参数 | 脚本 |
|------|------|
| Ps | `PS/gru_ps.py`、`PS/gru_ps_2.py`、`PS/gru_ps_train_2.sh` |
| WPS | `WPS/gru_wps_1.py`、`WPS/gru_wps_a800.sh` |
| Ts + Tm（联合） | `Ts_Tm/gru_ts_tm.py`、`gru_ts_tm_fixed.py`、`gru_ts_a800.py`、`gru_tm_a800.py`、`*.sh` |
| 早期尝试 | `gru_cnn.py`、`test2.py`（LSTM/LSTM+CNN/BiLSTM 等，效果一般后放弃） |

### 3.3 结果文件（`第一阶段/result/`）
- `train_stations_info.txt`（329 站）/ `test_stations_no_training_info.txt`（36 站，含站名/经纬度/高程/起止年）
- `ps_result/gru/`：`gru_model.pth`、`model_preds.npy/.txt`、`model_preds_with_info.txt`（Station ID / Time Index / Pred / Actual / Error）、`scatter_gru_test.png`
- `wps_result/gru/`、`ts_tm_result/gru/`：模型权重 + `model_preds.npy`（Ts/Tm 为 2 列）
- `*_result/empirical_validation/`：经验验证用的 X/y npy 文件

> ⚠️ **注意**：PS 的 `model_preds_with_info.txt` 中实际值列存在大量 NaN（约一半），直接按行计算指标会得到错误结果（RMSE≈36 hPa 明显异常）。**第一阶段各参数精确指标建议从服务器训练日志提取，或按站点/时间索引把 `model_preds` 与测试集真值重新对齐后计算**。PPT 中已有 4 参数对比图。

### 3.4 遗留事项
- `第一阶段/.git` 有大量未提交改动（新脚本未 add/commit），建议整理后提交；
- `xg_r/` 为空，确认是否已废弃。

---

## 4. 第二阶段总结（本次完成）

### 4.1 目标（核心创新）
基于 Transformer 的廓线序列模型，直接从 ZWD + 探空廓线预测转换系数 Pi（=PWV/ZWD），**完全不用 Tm**，摆脱传统 Saastamoinen 对 Tm 的线性假设。

### 4.2 最终文件清单（`第二阶段/`）

| 文件 | 职责 | 状态 |
|------|------|------|
| `data.py` | 读探空→变长廓线→归一化→站点切分→DataLoader | ✅ 已跑通 |
| `model.py` | `ProfileTransformer`（连续高度位置编码 + CLS + Pre-LN 4层8头） | ✅ 已跑通 |
| `train.py` | 训练（Huber 损失 + 余弦退火 + 早停 + 测试评估 + 出图） | ✅ 已跑通 |
| `evaluate.py` | Transformer vs Saastamoinen 全流程/Pi-only 对比评估 | ✅ 已跑通 |
| `loso_evaluate.py` | **新增**：留一站交叉验证（小数据稳健评估），支持 `--test_stations` 消融 | ✅ 已跑通 |
| `run_train.sh` | 服务器 SLURM 提交脚本（已把 `#SLURM` 修正为 `#SBATCH`） | ⚠️ 需改 `DATA_DIR` |
| `README.md` | 更新后的完整说明（含实测结果） | ✅ |
| `requirements.txt` | 依赖清单（新增） | ✅ |
| `result_v2/` | 单次切分训练结果（10 文件：模型/指标/图表/scalers.pkl） | ✅ |
| `result_loso/` | LOSO 交叉验证结果（6 文件：对比指标/逐折/散点/误差分布） | ✅ |

### 4.3 本次修复与改进

| 类别 | 内容 |
|------|------|
| Bug 修复 | ① `train.py` 文档字符串 `\x` 转义导致 SyntaxError；② `run_train.sh` 的 SLURM 指令 `#SLURM`→`#SBATCH`（原指令不会被 SLURM 识别）；③ torch 2.6+ `weights_only=True` 无法加载含 sklearn scaler 的 checkpoint → scalers 单独存 `scalers.pkl` |
| 功能增强 | 训练脚本新增逐站点指标输出；`evaluate.py` 自动加载 `scalers.pkl`（缺失时回退重建）；数据管线为廓线记录补充 `tm_surface` |
| 新增能力 | `loso_evaluate.py` 留一站交叉验证；`--test_stations` 快速消融选项 |
| 文档 | README 重写（结果、命令、注意事项）；新增 requirements.txt |

### 4.4 本地实测结果

**LOSO 留一站交叉验证**（每折 9 站训练、20 epochs、patience 6，10 折聚合，N=4077）：

| 方法 | RMSE (mm) | MAE (mm) | R2 | Bias (mm) |
|------|----------:|---------:|-----:|----------:|
| Saastamoinen（全流程，从 P/T/e 算 ZWD） | 8.40 | 5.83 | 0.68 | +1.06 |
| Saastamoinen（Pi-only，用**真值** Tm） | 0.24 | 0.18 | 0.9997 | +0.10 |
| **ProfileTransformer（LOSO，不用 Tm）** | **0.39** | **0.25** | **0.9993** | −0.09 |

逐折测试站 RMSE（mm）：AEM=0.43，AFM=0.32，AGM60390=0.26，AGM60571=0.19，AGM60630=0.15，AGM60656=0.16，AGM60680=0.29，AMM=0.17，**AQM=0.75（最难，热带湿润站）**，ARM=0.32。

**单次切分**（1 个测试站 AQM00091765，其余 9 站训练）：Transformer RMSE=1.87 mm（Bias +1.76），Pi-only 0.31 mm。
> ⚠️ 单次切分只由 1 个站决定，不代表模型水平，**评估请以 LOSO 为准**。

### 4.5 关键发现（对论文/PPT 有价值）

1. **线性 Tm 公式存在系统性、随站点变化的残差**：数据中真值 Pi(=PWV/ZWD) 与 Saastamoinen 公式 Π(Tm) 相差约 0.002（≈0.2–0.6 mm PWV），各站偏差 +0.001~+0.0025 不等。这正是"摆脱 Tm 线性假设"的明确靶子。
2. **模型完全不用 Tm 就能逼近"真值 Tm"oracle**：LOSO 0.39 mm vs 0.24 mm。真实部署时 Tm 来自 GPT3/第一阶段 GRU（本身有误差），Transformer 优势会更明显；相对传统全流程（8.40 mm）提升约 95%。
3. **站间气候泛化是主要难点**：最难测试站 AQM（萨摩亚，热带湿润，ZWD 最高约 410 mm）、ARM（阿根廷亚热带）均为训练站里少见的湿润气候 → 模型存在系统性 Pi 偏差（约 +0.002 量级）。**服务器 329 站全球覆盖后预期显著改善**。
4. **消融结论**：把地表 T/P/e 直接加入全局特征在小数据下反而恶化泛化（AQM 折 0.75→1.87 mm），已回滚。廓线本身已含地表信息，模型可通过注意力提取，维持 9 维全局特征设计。

---

## 5. 运行手册（交接必读）

### 5.1 本地快速验证（10 站）

```bash
cd D:\gnss水汽反演\第二阶段
# 用 torch_gpu 环境
D:\Users\lenovo\anaconda3\envs\torch_gpu\python.exe data.py          # 数据管线自检
D:\Users\lenovo\anaconda3\envs\torch_gpu\python.exe train.py --data_dir D:/gnss水汽反演/第一阶段/xg_test --epochs 20 --patience 6
D:\Users\lenovo\anaconda3\envs\torch_gpu\python.exe evaluate.py --data_dir D:/gnss水汽反演/第一阶段/xg_test --model_path result/best_model.pth
```

### 5.2 本地稳健评估（推荐）

```bash
D:\Users\lenovo\anaconda3\envs\torch_gpu\python.exe loso_evaluate.py --data_dir D:/gnss水汽反演/第一阶段/xg_test --output_dir result_loso
# 快速消融（只跑指定站）：
# ... loso_evaluate.py ... --test_stations AQM00091765,ARM00087155
```

### 5.3 服务器全量训练（关键步骤）

1. 把 329 训练站 + 36 测试站数据上传到服务器；
2. 编辑 `run_train.sh`：把 `DATA_DIR` 改成服务器实际路径（当前是占位符 `/home/user/gnss_pwv/xg_data`）；
3. 提交：`sbatch run_train.sh`（会自动依次跑 `train.py` 和 `evaluate.py`）。

### 5.4 输出文件说明

- `best_model.pth`（模型权重）、`scalers.pkl`（归一化参数，评估时自动加载）
- `metrics.txt`、`test_predictions.csv`、`per_station_metrics.csv`
- `comparison_metrics.csv`（三方法对比）、`comparison_scatter.png`、`error_distribution.png`
- LOSO 额外输出：`per_fold_metrics.csv`、`per_fold_rmse.png`

---

## 6. 下一阶段预览与交接（重点）

### 6.1 立即可做的待办（服务器）

| 优先级 | 事项 | 说明 | 状态 |
|--------|------|------|------|
| P0 | 全量数据训练 | 335 训练站 / 37 测试站，2014–2019，100 轮，L40 GPU，2h54m（作业 2088104） | ✅ 已完成 |
| P0 | 检查热带/湿润站泛化 | 37 个测试站最大 RMSE 仅 0.123 mm（BRM00082332），站间泛化问题基本解决 | ✅ 已完成 |
| P1 | 与 Pi-only oracle 对比 | **Transformer 0.0545 mm 显著反超真值 Tm 基线 0.257 mm（+78.8%）**，创新点成立 | ✅ 已完成 |
| P1 | 与 GPT3 基线对比 | **官方36站: Transformer 0.0653 vs GPT3 0.3450 mm → 提升 81.1%**（GPT3 Tm 误差 RMSE=5.84K）。详见总结文档 §10 | ✅ 已完成 |
| P1 | 与第一阶段官方 36 站切分对齐 | **已用官方 36 测试站重训+评估**（作业 2088911，RMSE 0.0653 mm），data.py 已支持 `--test_stations` | ✅ 已完成 |
| P1 | 第三阶段分层/极端分析 | 季节/纬度带/ZWD 分层 + 极端场景（特湿/强变化）分析均完成，极端场景相对 GPT3 提升 75–82% | ✅ 已完成 |
| P1 | 全球格网化 Pi 产品（创新点三） | **5°×5°×4季 全球 Pi 格网已生成**（0.136–0.171，极地→热带递增），`grid_product.py` + `result_grid_aligned/` | ✅ 原型完成 |
| P2 | 端到端实验 | 用第一阶段 GRU 预测的 Ps/Ts/WPS 替代探空实测做推理输入 | ⏳ 待办 |
| P2 | 模型优化 | Optuna 超参、辅助 Tm 多任务头、XGBoost 残差修正 | ⏳ 待办 |

### 6.2 第三阶段方向（PPT 规划：2026.06–12 模型优化 / 全球四季验证 / 误差分析与订正）

1. **模型优化**（按优先级）：
   - 辅助任务：加 Tm 预测头做多任务正则（训练时用 Tm、推理不用，不破坏创新点）；
   - 沿用第一阶段的 **Optuna 超参搜索 + XGBoost 残差修正** 思路做第二阶段；
   - 更大/更优架构：d_model、层数、dropout 扫描；多折集成；
   - 输入增强：把第一阶段 GRU 预测的 Ps/Ts/WPS（替代探空实测）作为输入，做"准端到端"实验；
   - 背景廓线：评估用 GPT3/ERA5 廓线替代探空廓线作推理输入的可行性（部署场景）。
2. **全球四季验证**：
   - 按纬度带（极地/中纬/热带）、气候区、季节分层统计 RMSE/偏差；
   - 重点覆盖复杂场景：台风、暴雨、寒潮、山区/沿海站。
3. **误差分析与订正**：
   - 逐站偏差（bias）订正：固定 GNSS 站可用历史数据做站级校准；
   - 分析 Pi 残差与 Tm 公式偏差的关系（验证发现 1）；
   - 误差随 ZWD/季节/站点高程的变化规律。
4. **创新点三落地**：把训练好的模型在经纬度网格上推理，生成全球格网化转换系数产品。

### 6.3 成果材料准备（论文/PPT/专利）

- 需要补充的核心图表：
  - 三方法（传统全流程 / Pi-only / Transformer）对比散点与指标表（本地已有模板图）；
  - LOSO 逐折柱状图（已有模板）；
  - 分季节、分纬度带、分气候区误差图（需全量数据）；
  - 与 GPT3 对比图（需补 GPT3 基线）；
  - 典型站时间序列对比（选 1 个干旱站 + 1 个湿润站）。
- 专利/软著：直接映射模型架构 + 数据驱动转换系数方案可作为素材。

### 6.4 风险与注意事项

1. **小数据陷阱**：本地 10 站结论仅供流程验证，正式结论以 329 站为准；评估务必用"留站点"切分（不能按时间随机切，否则站点泄漏会虚高）。
2. **torch 版本**：服务器若 torch < 2.6，`weights_only` 行为不同；代码已用 `scalers.pkl` 规避。
3. **编码问题**：Windows 下中文路径 + Python stdin 管道会乱码；写脚本请用 UTF-8 文件再执行，避免 BOM（bash 脚本有 BOM 会导致 shebang 失效）。
4. **文件清理**：本环境 PowerShell 的 `Remove-Item` 受策略限制，清理文件建议用 Python `os.remove/shutil.rmtree`。
5. **git**：`第一阶段/.git` 有未提交改动；根目录与 `第二阶段/` 未纳入 git，建议统一建仓管理。
6. **环境变量**：服务器跑 torch 若报 OpenMP 冲突，设 `KMP_DUPLICATE_LIB_OK=TRUE`。

### 6.5 交接清单（负责人核对）

- [ ] 已能本地复现：`data.py` → `train.py` → `evaluate.py` → `loso_evaluate.py`
- [ ] 已阅读 `第二阶段/README.md` 与本文档
- [ ] 已确认服务器数据路径并修改 `run_train.sh`
- [ ] 已提交 `sbatch run_train.sh` 并盯 `logs/` 输出
- [ ] 已记录全量训练结果（三方法对比 + 热带站专项）
- [ ] 已安排第三阶段模型优化 / GPT3 基线补测

---

## 7. 附录

### 7.1 关键命令速查

| 用途 | 命令 |
|------|------|
| 数据管线自检 | `python data.py` |
| 单次训练+评估 | `python train.py --data_dir <数据> --output_dir result` |
| 对比评估 | `python evaluate.py --data_dir <数据> --model_path result/best_model.pth` |
| LOSO 稳健评估 | `python loso_evaluate.py --data_dir <数据>` |
| 服务器提交 | `sbatch run_train.sh` |
| 传统基线批量评估 | `python eval_saastamoinen.py --data_dir <数据>` |

### 7.2 本机环境

- Python：`D:\Users\lenovo\anaconda3\envs\torch_gpu\python.exe`（torch 2.7.0+cu126，CUDA 可用）
- 依赖：torch / numpy / pandas / scikit-learn / matplotlib（详见 `第二阶段/requirements.txt`）

---

*本文档由 Codex 根据当前工作区文件、运行结果与项目材料整理生成；数据与结论以实际运行与服务器结果为准。*
