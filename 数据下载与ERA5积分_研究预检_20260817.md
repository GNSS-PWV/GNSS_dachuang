# GNSS / ERA5 数据与积分计算：下一阶段研究预检

> **日期：2026-08-17**  
> **状态：已完成离线预检；当前禁止直接启动批量下载。**  
> **范围：** GNSS 数据下载、ERA5 下载、ERA5 垂直积分代码，以及它们服务于下一阶段 GNSS 水汽反演研究的可行性。  
> **安全说明：** 本预检不读取、不复制、不显示任何认证材料；不联网、不下载、不提交外部任务。

---

## 1. 下一阶段真正要解决的研究问题

当前严格六年结果的主要边界之一，是部署侧使用的气候态垂直廓线来自空间留站、全历史 cache，时间未来性仍不足。

下一阶段应优先研究：

```text
以时间截止点之前的数据构建先验 / 使用 ERA5 或数值天气预报剖面，
在截止点之后的年份进行严格未来时段测试。
```

目标不是重复现有 `clim_surf_p1` 结果，而是验证：在不使用未来时段资料构建剖面先验的条件下，第一阶段预测的 PS / TS / WPS 与第二阶段 ProfileTransformer 是否仍能稳定优于 GPT3。

---

## 2. 已完成的离线预检

新增了不联网、不下载、不读取认证文件的预检脚本：

```text
数据计算代码/preflight_data_pipeline.py
```

它会检查：

- GNSS 下载脚本、ERA5 下载脚本和 ERA5 MATLAB 积分包是否存在；
- `gnss_downloader.py` 自定义实现是否存在于当前导入路径；
- GNSS 所需配置、站点列表、EGM96 文件是否存在；
- `boto3`、`cdsapi`、`pygeodesy`、`earthscope_sdk` 等 Python 依赖；
- Windows 输出路径是否可用；
- ERA5 积分压缩包是否含关键 MATLAB 文件；
- MATLAB 是否可用。

使用方法：

```powershell
python .\数据计算代码\preflight_data_pipeline.py
python .\数据计算代码\preflight_data_pipeline.py --json
python .\数据计算代码\preflight_data_pipeline.py --strict
```

其中 `--strict` 在存在阻塞项时返回退出码 `2`，适合后续自动化流程阻断。

---

## 3. 当前发现的阻塞项

### 3.1 GNSS 下载入口目前不能运行

`数据计算代码/download_gnss_stations.py` 依赖：

```text
src.data.download.gnss_downloader.GNSSDownloader
```

但当前工作区没有发现与该导入对应的 `gnss_downloader.py` 实现，也没有发现脚本默认所需的 `download_lables` 支撑目录。因此脚本在进入参数解析前就会因导入失败停止。

此外，当前还缺少：

- GNSS 默认配置；
- GNSS 站点列表；
- EGM96 大地水准面文件（该项可降级，但会影响 PWV 计算精度）；
- 脚本中延迟导入的部分辅助模块。

**结论：在补齐原始 GNSS 工具包之前，不能尝试 GNSS 下载。**

### 3.2 ERA5 下载脚本也尚未具备运行环境

当前 Python 环境已具备 `numpy`、`pandas`、`xarray`、`tqdm`，但：

- 公共 ERA5 下载脚本需要的 `boto3` 未安装；
- CDS ERA5 下载脚本需要的 `cdsapi` 未安装；
- GNSS 高程修正的可选依赖 `pygeodesy` 未安装；
- 认证型 GNSS 数据源的可选 SDK 未安装。

同时，两个 ERA5 下载脚本都有 Windows 路径适配问题：一个使用 POSIX 风格输出路径，另一个固定写入当前不存在的磁盘目录。未经改造直接运行，输出位置可能错误或直接失败。

### 3.3 下载脚本的空间、时间范围尚未与当前主实验对齐

当前正式 GNSS-PWV 部署结果覆盖 **2014–2019 年**、六年联合 36 个官方站。

但现有 ERA5 下载脚本的设计范围分别偏向：

- 2018–2024 年的较大范围公共数据；
- 2020–2024 年的香港区域数据。

因此，它们**不能原样作为当前 2014–2019 全球/跨区域严格时间外推实验的数据生产脚本**。后续必须先明确新的研究区域、年份和站点集合，再修改下载范围与输出组织方式。

### 3.4 ERA5 积分计算代码具备基础条件，但还没有可用输入

`ERA5_obtain.zip` 中已包含关键 MATLAB 文件，包括：

- `SHAtrop_layer.m`：按气压、温度、比湿和位势高度计算干、湿延迟及 Tm；
- `Inter_fromncfile_new.m`：读取 ERA5 压力层 NetCDF，逐时次输出积分结果。

本机检测到 MATLAB，可在准备好少量、格式匹配的 ERA5 NetCDF 文件后进行单文件积分验证。

但当前尚无经过确认的 ERA5 输入文件及统一的输出路径，因此不应直接批量运行积分。

---

## 4. VPN 状态

目前**尚未开始实际数据下载**，所以还没有产生可用于判断速度的下载记录。

因此当前结论是：

- 还不能判断是否需要 VPN；
- VPN 不能解决本地缺失的 GNSS 自定义包、配置目录、Python 依赖或 Windows 路径问题；
- 等前置问题解除后，先做单日、单源、小范围测试；如果出现连接超时、域名不可达或持续低速，再立即暂停并向用户报告 VPN/网络问题。

---

## 5. 推荐的执行顺序

### 阶段 A：补齐可运行环境（当前第一优先级）

1. 找回或补充完整的 `src/data/download/gnss_downloader.py` 及其依赖；
2. 找回与其版本匹配的 `download_lables` 目录，包括配置、站点列表和必要静态资源；
3. 新建隔离 Python 虚拟环境，固定并记录依赖版本；
4. 将两个 ERA5 脚本的输出路径改为项目内或明确的数据盘路径；
5. 再次运行 `preflight_data_pipeline.py --strict`，直到没有关键阻塞项。

### 阶段 B：最小联网验证（不启动大规模下载）

GNSS 的第一条实际联网任务建议限制为：

- 单日；
- 单一免认证源；
- 小经纬度范围；
- `--no_pwv`；
- 隔离临时输出目录；
- 禁止认证型数据源；
- 记录下载耗时、文件数、样本数和失败原因。

ERA5 的第一条任务应是：

- 一个日期或一个月；
- 一个变量组；
- 一个很小的空间范围；
- 明确的本地输出目录；
- 下载后先检查 NetCDF 变量、维度、时间轴与单位；
- 再调用 MATLAB 积分代码处理单个文件。

### 阶段 C：形成新的严格未来时段实验

在数据链路打通后，再设计并执行：

```text
训练/先验构建：截止年及之前
验证：中间年份
测试：后续未参与先验构建的年份
```

同时保持：

- P1→P2 的 `(station, TIME)` 精确匹配；
- 各比较方法的共同样本口径；
- 不使用目标未来分布筛样；
- 年度、站点、季节和气候区分层报告；
- 与 GPT3、纯气候态和现有 `clim_surf_p1` 的对照。

---

## 6. 当前可验证结论

1. 现有下载和积分代码没有明显 Python 语法错误；
2. 目前最大的阻塞不是 VPN，而是缺少 GNSS 自定义下载包、支撑目录、下载依赖及路径适配；
3. 本机已经具备后续 MATLAB ERA5 积分验证的运行条件；
4. 下一步不应直接跑多年或多账号下载，而应先补齐代码包并通过离线预检；
5. 现有 ERA5 下载脚本的年份/区域与当前 2014–2019 严格部署研究并不完全匹配，必须先重新定义数据范围。
