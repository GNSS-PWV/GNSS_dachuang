# 当前任务计划：2019 美国/澳大利亚独立验证

更新时间：2026-09-01

## 已完成

- 已读取 `D:\myai\AGENTS.md`，按难度/风险进行本地与子任务分工。
- 已审计 `2019_US_Australia_IGS_ERA5`，文件 SHA-256 校验通过。
- 已确认数据覆盖：2019 年 1/4/7/10 月各 1-10 日，共 40 天；澳洲 22 站；美国 43 站。
- 已确认 ERA5 单层变量：`t2m`、`sp`、`tcwv`，以及静态 `geopotential`；没有气压层完整 profile。
- 已确认 GNSS 日 CSV 为 5 分钟 IGS ZTD，ZTD 与站点经纬度/椭球高齐全。
- 已新增并通过烟雾测试：`第二阶段/prepare_2019_gnss_era5_independent.py`。
- 已完成全量预处理：609,984 行中 606,839 行通过质控；产物位于 `第二阶段/independent_2019_us_australia_era5_v1/`。
- 预处理脚本和计划已推送 GitHub：提交 `d451060`；派生数据未提交 Git。

## 当前步骤

1. 复核全量预处理结果中的 2,088 条 ZWD 范围异常与 1,060 条半小时边界时刻，确定后续保留/聚合规则。
2. 生成可供服务器复算的最小数据上传清单。

## 后续步骤

1. 审核全量预处理报告，重点检查负 ZWD、时间偏差、高程差和站点覆盖。
2. 将独立 GNSS ZWD 与因果一阶段 `ELV/TS/PS/WPS` 预测 profile 对齐。
3. 获取或确认独立 PWV 标签，不能把 `tcwv` 自动当成最终真值。
4. 组装 `phase2_independent_contract_schema.json` 要求的样本契约并通过预检。
5. 预检通过后再上传/同步到同济或魔搭 GPU 环境，提交二阶段正式训练。
6. 对 2019 四季区域分别报告覆盖率、PWV/ZWD/Pi 误差、站点宏平均和极端样本。

## 当前阻塞与边界

- 当前 ERA5 文件没有气压层温度、比湿和位势高度 profile，不能直接充当二阶段 profile 输入。
- `tcwv` 是 ERA5 模式产品，暂定用于一致性参考，不是独立 PWV 标签。
- ZHD 当前采用 ERA5 `sp` + `z/g` 高程及 Saastamoinen/标准大气改正的临时实现；正式实验前需确认站高基准和 ZHD 口径。
- 旧二阶段 `0.05--0.4 mm` 结果仍只能称为 oracle/closed-loop pipeline validation。
- 2019 区域数据只覆盖 40 天，不能写成全年 2019 独立验证。

## 需要老师确认/提供

- 正式 PWV 标签来源、时间匹配容差、空间匹配规则和质控阈值。
- 正式 ZHD 公式、气压来源、站高基准及高程改正方法。
- 二阶段 profile 是采用因果一阶段预测，还是另行提供历史/实时 NWP profile。
- 若要求真正 profile 驱动，需要补充四维 pressure-level ERA5：温度、比湿、位势高度和压力层列表。
