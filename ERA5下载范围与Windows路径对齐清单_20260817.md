# ERA5 下载范围与 Windows 路径对齐清单（2026-08-17）

> 本清单只审计现有非认证下载脚本中的静态时间、空间、输出路径与依赖约束；未联网、未读取认证材料、未下载任何文件。

## 1. 当前正式研究需求

当前可复核的正式部署结果覆盖 **2014–2019 年**。下一阶段若要验证真实部署外推，应采用明确的“训练截止年 → 截止年之后”时间分割，并在开始下载前固定：

1. 训练截止年份和独立未来验证年份；
2. 站点覆盖所需的空间范围；
3. ERA5 产品、层次（尤其压力层）、变量、时间分辨率和单位；
4. 输出目录、磁盘预算、文件命名与断点续传/完整性检查；
5. 与 MATLAB `SHAtrop_layer.m` / `Inter_fromncfile_new.m` 的 NetCDF 输入兼容性。

这些项目目前尚未形成可执行且与主实验完全对齐的下载清单，因此禁止直接批量运行现有脚本。

## 2. 现有脚本的静态事实

| 脚本 | 代码中的范围/路径 | 与当前研究的关系 | 当前状态 |
|---|---|---|---|
| `download_era5_2018_2024.py` | 全球单层数据；2018-01-01 至 2024-12-31；输出 `/f/era5_single_level` | 缺少 2014–2017，且“全球单层”未确认能满足压力层积分需求；POSIX 路径不适合当前 Windows 环境 | 不可直接运行 |
| `download_era5_hk_cds.py` | 香港周边区域 `[26.5, 108.0, 18.0, 119.0]`；2020–2024；输出 `F:/era5_hk_cds` | 年份晚于当前 2014–2019 主结果，空间仅限香港周边；当前机器无 F: 盘 | 不可直接运行 |

本机当前可用文件系统空间（2026-08-17 预检）：C: 约 16.7 GiB，D: 约 47.5 GiB。任何多年 ERA5 批量方案必须先计算体积并确认磁盘预算；不能假定当前可用空间足够。

## 3. 下载启动的前置条件

在以下事项全部明确前，保持下载停止：

- [ ] 找到或补齐 GNSS 下载器与匹配的站点配置，以便确认实际站点空间覆盖；
- [ ] 建立隔离 Python 环境，并安装、固定和记录 ERA5 所需依赖版本；
- [ ] 将两个 ERA5 脚本改为项目内明确的 Windows 输出目录，禁止写入不存在的 F: 或 POSIX `/f` 路径；
- [ ] 确认需要的是单层、压力层或二者；ERA5 积分链路至少需要经验证的压力层 NetCDF 输入；
- [ ] 明确时间分割，不将全时期资料误用于“训练截止年后”的独立未来验证；
- [ ] 为单次试验定义最大文件数、最大下载量和停止条件。

## 4. 第一次网络试验的严格边界

前置条件满足后，第一轮只能是：

```text
单日 + 单源 + 小区域 + 最少必要变量 + 隔离临时目录
```

成功标准：

1. 可以完成请求并得到可读取的 NetCDF；
2. 核对变量、维度、时间坐标、单位、经纬度/层次；
3. 记录总字节数、耗时、有效吞吐量和错误信息；
4. 用 MATLAB 积分代码处理单个文件，确认结果单位与物理量合理；
5. 只在以上全部通过后，才讨论单月、单年或批量执行。

停止与 VPN 规则：若实际试验出现持续低速、连接超时、域名不可达、TLS 错误或认证失败，应立即停止该试验并向用户报告；此时再由用户决定是否开启或检查 VPN。当前尚未发起真实下载，因此没有任何当前速度结论。

## 5. 建议的后续文件接口

在范围确定后，建议创建一个不含认证信息的实验清单（例如 `era5_future_eval_manifest.yaml`），至少包含：

```yaml
experiment_id: <固定标识>
train_cutoff_year: <待确定>
evaluation_years: [<待确定>]
station_list_version: <待确定>
area_nwse: [<待确定>]
products: [pressure_level, single_level]
variables: [<待确定>]
hours_utc: ["00:00", "...", "23:00"]
output_root: <项目内Windows路径>
max_trial_bytes: <单次试验上限>
```

该清单应与预测生成配置一同归档，避免仅靠脚本中的硬编码年份、区域和路径复现研究。
