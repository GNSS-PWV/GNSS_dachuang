# 2019 年四季区域化验证：下载前执行清单（2026-08-19）

## 1. 目的与边界

本清单落实老师于 2026-08-19 提出的下一阶段建议：以 **2019 年 1、4、7、10 月** 覆盖四季；研究区域优先选择 **美国、欧洲、澳大利亚**；ERA5 不下载全球，而是按区域和日期下载温度、气压（或气压层坐标）、比湿和位势/位势高度；训练标签以探空气球资料为准，GNSS 只作为独立验证。

本文件只规定下载前准备、单日探针、验收和升级条件，不把任何探针输出纳入训练、模型选择或正式论文结果。

## 2. 当前已满足的本地条件

- GNSS 下载器默认所需的 `config_v2.yaml` 和五个站表已在 `数据计算代码/download_lables/` 下接入，原始老师文件仍保留在根目录 `文件/`。
- 离线预检由 5 个 BLOCKER 降为 3 个：缺少 `boto3`、缺少 `cdsapi`、CDS 脚本固定使用当前不可用的 `F:` 输出盘。
- EGM96/`pygeodesy` 是 PWV 高程转换精度告警；对仅下载或验证 ZTD 的首个 GNSS 探针不构成阻塞。
- `suominet` 需要 EarthScope OAuth2；本阶段不读取、不配置、不使用认证材料。

## 3. 站表质量口径

下载器的格式为 `SITE_ID,LAT,LON,HEIGHT`。四个站表（IGS、EUREF、澳大利亚、SuomiNet）可被现有解析器读取；但 UCAR 表存在：

1. 一条表头样式非数值行（生产解析器会跳过）；
2. 一条纬度越界、字段疑似错位的记录；
3. 9 个重复站点 ID 的额外重复项。

因此，在 UCAR 正式启用前，必须由离线审计报告锁定问题，并在**不改动老师原始站表**的前提下生成候选清洗副本；重复 ID 的保留规则需根据原始数据源或老师确认。不得把下载器“未报错”当作 UCAR 站表已通过质量验收。

审计命令（只读输入；可生成 JSON 报告）：

```powershell
python 数据计算代码/validate_station_lists_offline.py `
  --station-list-dir 数据计算代码/download_lables/GNSS_list `
  --output 数据计算代码/validation_reports/station_list_audit_20260819.json
```

## 4. 区域与数据源顺序

| 目标区域 | 首选 GNSS 源 | 产品 | 首次下载建议 | 当前限制 |
|---|---|---|---|---|
| 欧洲 | `euref` | ZTD | 2019-01-01 单日、单源、`--no_pwv` | 需外网/VPN；需检查真实返回记录 |
| 澳大利亚 | `australia` | ZTD | 2019-01-01 单日、单源、`--no_pwv` | 需外网/VPN；需检查真实返回记录 |
| 美国 CONUS | `ucar_cosmic` | 直接 PWV | UCAR 清洗表经确认后再单日探针 | 站表当前为条件性通过 |
| 全球补充 | `igs_whu` | ZTD | 最后才做，且必须给出经纬度范围和低并发 | 逐站逐日，体量和限速风险较大 |
| 美国高密度 | `suominet` | ZTD | 暂不启用 | 需要 OAuth2 认证 |

建议首个联网探针优先选择 EUREF，而不是 IGS：其为免认证区域源，不会触发 IGS 的大规模逐站循环，也不会触碰 UCAR 的站表清洗问题。

## 5. 首次联网 GNSS 探针（必须先连接外网/VPN）

**开始前必须由用户确认 VPN/外网已连接。** 本机即可执行，无需同济超算。建议工作目录为 `数据计算代码`，并使用从未使用过的隔离输出目录：

```powershell
python download_gnss_stations.py `
  --start 20190101 --end 20190101 `
  --sources euref `
  --no_pwv `
  --output_dir data/raw/validation/GNSS_probe_20190101_euref `
  --config_path download_lables/config_v2.yaml `
  --station_list_dir download_lables/GNSS_list
```

注意：不能只以退出码或 `_done.json` 判断成功，因为现有来源循环可能将空结果也记为完成。探针通过必须同时满足：

1. 至少一个按站点输出的 CSV 文件；
2. 每个抽查文件有可解析的 `datetime` 和 `ztd_mm`（或 UCAR 的直接 `pwv_mm`）字段；
3. 记录数大于零，时间包含 2019-01-01；
4. 输出站点经纬度/来源与目标区域一致；
5. 下载日志未出现认证跳过、系统性 HTTP/FTP 失败或解析全空；
6. 保存下载命令、运行时间、文件数、有效行数、失败原因和样本 SHA256。

若下载速度异常慢、网络错误持续或出现限速，立即停止，不批量重试，并请用户检查 VPN/外网状态。

## 6. ERA5 单日工程准备（尚不执行下载）

ERA5 路线必须先在以下两者中选一，并完成最小可逆改造/验证：

- 公共 S3：需安装 `boto3`，并将 Windows 下的 POSIX 风格输出路径参数化；
- CDS：需安装 `cdsapi`，并消除脚本固定 `F:` 输出盘；认证材料始终由用户在受控环境配置，代理不读取或输出认证文件。

数据产品与积分输入需在真实 API/文件样本上确认变量名称和维度。目标变量为：

- 温度；
- 气压（或 pressure-level 坐标）；
- 比湿；
- 位势或位势高度。

在下载 1、4、7、10 月前，应先完成：**2019-01-01、单区域、单日的完整层次小样本**，并用 `ERA5_obtain` 的 MATLAB 程序检查 NetCDF 变量、维度、时间、层序和输出单位。没有真实 NetCDF 输入时不得宣称 ERA5 积分已验证。

## 7. 四季批量阶段的冻结条件

只有当下列条件全通过后，才允许开始每月批处理：

1. 每个区域至少一个 GNSS 单日探针通过验收；
2. ERA5 路线、磁盘目录、变量名、层次、单日积分均已验证；
3. 对每个区域写明经纬度框与日期清单，避免全球下载；
4. 估算并确认磁盘容量、下载次数、失败重试规则和数据校验方式；
5. 冻结产物目录，防止探针、训练和正式评估文件混放；
6. 明确探空气球训练样本的位置、字段、时间/空间匹配规则，以及与 GNSS 验证集的隔离规则。

建议月份分批顺序：2019-01 → 2019-04 → 2019-07 → 2019-10；每一批完成后均进行文件、时间、变量、空间范围和积分输出的验收，再进入下一批。

## 8. 训练与同济超算的启动条件

目前**不应启动训练，也暂不需要连接同济超算**。以下条件全部满足后，再请求连接超算，并提交明确资源预算（GPU 型号/显存、CPU、内存、磁盘、预计训练轮数和时长）：

1. 探空气球训练数据已定位、可读取、质量规则已冻结；
2. ERA5 单日到四季数据链路和积分输出均已通过；
3. GNSS 验证数据与训练样本严格隔离；
4. 第二阶段输入、训练/验证/测试时间切分、基线模型和评价指标已冻结；
5. 已写出一次小规模 dry-run 的资源需求与失败恢复流程。

## 9. 需向老师/数据提供方追问的最小问题

如果 UCAR 被确定为美国正式验证源，建议确认：异常记录应如何处理、重复 ID 是否代表不同坐标/时间版本、以及老师期望的去重规则。除此之外，训练启动前仍需要探空气球训练数据的确定文件清单与字段说明。
