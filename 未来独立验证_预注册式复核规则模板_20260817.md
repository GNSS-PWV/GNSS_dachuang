# 训练截止年后未来独立验证：预注册式复核规则模板（2026-08-17）

> 状态：**研究设计模板，尚未执行。** 必须在下载、生成未来期预测或查看未来期误差之前填写并冻结尖括号中的字段。本文不含认证信息。

## 1. 目的

验证现有两阶段方案在训练截止年之后的独立时期是否仍优于 GPT3，同时将当前 2014–2019 评估中事后发现的弱站作为**探索性、固定名单的复核对象**，而不是用于挑选测试集上的最优模型。

## 2. 冻结字段（必须先填写）

```yaml
experiment_id: <例如 p1deploy_future_YYYYMMDD>
protocol_version: 1
freeze_date: <YYYY-MM-DD>
train_cutoff_year: <YYYY>
evaluation_years: [<YYYY>, <YYYY>]
station_list_version: <固定站点清单文件与哈希>
station_population_rule: <未来期所有满足独立数据质量规则的目标官方站>
era5_product_and_variables: <产品、压力层/单层、变量、单位>
era5_area_nwse: [<north>, <west>, <south>, <east>]
gnss_source_and_version: <来源与版本>
output_root: <项目内Windows绝对路径>
```

冻结后不得因为某个站点、年份或模型表现不佳而缩小时间范围、删除站点或改变主指标；如确需修改，必须新建协议版本并同时报告旧版本结果。

## 3. 预先定义的比较对象与主指标

### 3.1 主比较

```text
候选可部署方案：clim_surf_p1
基线：GPT3
共同样本：同一时刻、同一站点，且 pwv_true、clim_surf_p1、GPT3 均为有限数值
主指标：共同样本 micro-average RMSE（mm）
辅助指标：MAE、Bias、站点级 RMSE、站点年度/季节级 RMSE
改进定义：GPT3 RMSE − clim_surf_p1 RMSE；正值代表 clim_surf_p1 更优
```

`real`、`real_surf_p1` 等 oracle 方案只能作为科学能力上限对照，不能作为部署精度或主比较结论。Tm 不是二阶段输入，若输出 Tm 统计只能标记为诊断量。

### 3.2 数据质量和剔除规则

在开始统计前固定：

1. 时间解析与时区规则；
2. GNSS、ERA5/NWP 与探空/真值的时空匹配容差；
3. 缺测、非数值与无穷值处理规则；
4. 去重规则；
5. 最低站点样本数阈值；
6. 被排除站点和记录的原因及数量报告格式。

所有模型必须应用相同的共同样本掩码。站点是否纳入总体由上述质量规则决定，不能由模型 RMSE 的优劣决定。

## 4. 当前探索性弱站名单的独立复核规则

截至 **2026 年 8 月 17 日**，当前 2014–2019 评估期中 `clim_surf_p1` 未优于 GPT3 的站点为：

```text
COM00080222
SFM00068842
SAM00040417
```

未来独立验证中：

- 该名单应原样固定为“探索性复核子组”；
- 同时必须报告全部预先定义目标站点的总体结果、逐站结果和可用性情况；
- 不得只报告这三个站点，也不得因未来表现调整名单后再称为验证结论；
- 对 PS、TS、WPS、Tm 的输入误差可做关联诊断，但只能写为描述性或假设生成性发现，不能单独推断因果。

## 5. 结果文件模板

建议至少生成以下文件，所有文件均应包含 `experiment_id`、协议版本、数据版本、生成时间和输入文件清单/哈希：

```text
future_eval_metrics_global.csv
future_eval_metrics_per_station.csv
future_eval_metrics_per_station_year.csv
future_eval_input_diagnostics.csv
future_eval_exclusions.csv
future_eval_manifest_frozen.yaml
```

### 5.1 站点级最小字段

```csv
experiment_id,protocol_version,station,N_common,rmse_clim_surf_p1_mm,mae_clim_surf_p1_mm,bias_clim_surf_p1_mm,rmse_gpt3_mm,mae_gpt3_mm,bias_gpt3_mm,gpt3_minus_clim_surf_p1_rmse_mm,surf_vs_gpt3,is_exploratory_weak_station
```

### 5.2 输入诊断最小字段

```csv
experiment_id,protocol_version,station,pwv_common_N,N_ps,rmse_ps_p1,mae_ps_p1,bias_ps_p1,N_ts,rmse_ts_p1,mae_ts_p1,bias_ts_p1,N_wps,rmse_wps_p1,mae_wps_p1,bias_wps_p1,N_tm,rmse_tm_p1,mae_tm_p1,bias_tm_p1,tm_diagnostic_only
```

其中所有 `bias_*_p1` 均定义为 `P1 − true`；`tm_diagnostic_only` 必须恒为 `true`。

## 6. 下载与积分门槛

只有在以下条件同时满足时，才允许开始首个单日网络试验：

- GNSS 下载器、配置、站点清单和静态资源已补齐且预检无关键阻塞；
- ERA5 输出路径可用于当前 Windows 机器；
- 产品、区域、时间、层次、变量与本协议完全一致；
- 依赖环境和版本已记录；
- 单次试验的文件数、最大字节数、停止条件已写入冻结清单。

如实际下载出现持续低速、超时、域名不可达、TLS 或认证失败，应停止测试并向用户报告，再决定是否开启 VPN；在此之前不应启动多年或大范围下载。
