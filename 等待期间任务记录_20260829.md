# Waiting-period work record (2026-08-29)

## Active job

- Job `2362126`: causal phase-one PS full retraining.
- The last reachable state was `RUNNING` on `gpu4006`, with CUDA `NVIDIA L40`.
- It was processing 363 profile files and had reached 360 files before the
  local monitoring session was interrupted.
- No cancellation was issued.

## Latest update (2026-08-30)

- PS job `2362126` completed successfully; metrics are recorded in
  `第一阶段/因果PS重训结果_20260830.md`.
- WPS job `2363862` was confirmed running on `gpu4004` and had reached
  `350/363` files before the latest monitoring attempt.
- The latest SSH/VPN checks timed out, so the WPS terminal state and result
  files are not yet verified. Do not submit another WPS job until that check
  succeeds.

## Parallel work completed

- Added batched evaluation to prevent the previous 187.77 GiB evaluation OOM.
- Added result-side scaler persistence and split/year metadata.
- Added an automatic result-directory validator.
- The data remains on the server; local deletion is independent and can wait
  until server integrity is reconfirmed.

## After job completion

1. Run `validate_causal_phase1_result.py` on the result directory.
2. Inspect test-2017, validation-2018 and held-station metrics in physical
   units (`PS` in hPa).
3. Only after PS passes validation, submit WPS, TS and Tm separately.
4. Do not use these phase-one results as evidence of independent phase-two
   GNSS-PWV accuracy; independent GNSS ZWD and PWV labels remain required.
