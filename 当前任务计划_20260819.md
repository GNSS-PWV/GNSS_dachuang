# 当前任务计划（2026-08-19）

## 当前阶段

完成老师新增资料的本地接入、离线审计和 2019 年四季区域化验证准备。当前不启动模型训练，不连接同济超算，不进行大范围下载。

## 已完成

1. 读取并遵循 `D:\myai\AGENTS.md`。
2. 接入老师提供的 `config_v2.yaml` 和五个 GNSS 站表。
3. 完成 SHA256 一致性校验和站表离线审计。
4. 完成 2019 年 1、4、7、10 月、美国/欧洲/澳大利亚区域化验证清单。
5. 完成主要 Python 脚本语法检查。
6. 更新项目进展检查点。

## 当前结果

- IGS、EUREF、澳大利亚、SuomiNet 站表：可被当前解析器读取。
- UCAR 站表：条件性通过；存在 1 条非数值行、1 条经纬度异常记录和 9 个重复站点 ID，需要确认清洗口径。
- 数据管线预检：`BLOCKER=3，WARN=5`。
- ERA5 目前尚无真实 NetCDF 输入，ERA5 积分尚未完成实测验证。- ERA5 CDS 脚本已增加无认证 `--dry-run`、`--area`、`--days`、`--output-dir` 参数；默认输出改为工作区内 `数据计算代码/data/raw/era5_targeted_cds`，尚未发起真实 CDS 请求。
- 当前环境仍缺少 `cdsapi` 与 `boto3`，真实 ERA5 下载保持阻塞；未读取 `ERA5下载key.txt`。

## EUREF 探针验收结果（2026-08-19）

首轮探针因 2019 年 EUREF 文件命名规则不兼容而得到 `0/15`，不是 VPN 故障。服务器实际存在 `EPNE03` 文件；下载器已以最小范围兼容 `EPNFIN` 与 `EPNE03` 两种明确命名。修复后重新合并得到：

- 9/15 个分析中心原始压缩文件成功下载；
- 8883 条原始解析记录、371 个解析站点；
- 按本地站表过滤后保留 7421 条有效记录、310 个站点 CSV；
- 308 个站点有 24 条小时记录；PLND 有 18 条，ZECK 有 11 条，属于源数据时间缺口；
- 所有记录字段为 `datetime,ztd_mm`，时间可解析，无重复时间、非数值 ZTD 或异常范围值；
- 原始目录有 9 个可解压 `.gz`，无残留 `.part`；
- 结构验收报告：`数据计算代码/validation_reports/euref_probe_audit_20260819.json`。

该结果是下载/解析链路探针，不是模型精度或正式四季验证结果。

## 下一步顺序

1. 保留并归档本次 EUREF 探针及验收报告，不把 `_done.json` 单独视为完整成功。
2. 明确 ERA5 下载路线：优先单日、单区域、分层变量小样本；先修复 CDS 脚本输出路径和依赖问题，不下载全球数据。
3. 用真实 ERA5 NetCDF 验证 MATLAB 积分代码的变量、维度、层序、时间和单位。
4. 完成探空气球训练数据的字段、站点坐标、单位和时间对齐后，建立训练/验证/测试切分。
5. 在数据链路闭环、训练方案冻结后，先通知用户连接同济超算，再启动训练或重训练。
6. ERA5/GNSS 批量下载前仍按区域和月份逐级扩展；若下载持续过慢或失败，立即停止并报告 VPN/外网问题。

## 当前需要老师补充

最关键的是探空气球训练数据及其说明，详见：

```text
给老师的资料请求完整模板_20260819.md
```

此外，希望老师确认 UCAR 异常记录和重复站点的处理口径。

## 网络和安全边界

- 真实 GNSS/ERA5 下载前，先确认 VPN/外网连接。
- 不使用 SuomiNet 认证源，不读取或传输账号、密码、Token、密钥或认证文件。
- 不下载全球 ERA5；先做单日、单区域探针。
- 当前不需要同济超算。
