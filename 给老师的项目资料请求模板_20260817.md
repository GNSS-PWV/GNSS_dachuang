# 给老师的项目资料请求模板（2026-08-17）

老师您好，我们已经完成了二阶段“一阶段预测地表状态驱动新模型”的严格回放式部署评估。当前正式结果和离线分析可以继续推进，但如果要启动 GNSS/ERA5 的新数据下载、积分和后续独立验证，还缺少原始数据处理目录中的几项资源。

想请您帮忙确认一下这些文件是否在原始项目包、服务器或其他目录中；如果有，提供目录路径或将对应的非敏感文件/文件夹复制到项目中即可：

## 目前最需要的文件

1. GNSS 下载配置文件：

   ```text
   download_lables/config_v2.yaml
   ```

2. GNSS 站点清单目录，至少需要 IGS 站点清单：

   ```text
   download_lables/GNSS_list/IGSwhu_formatted.txt
   ```

   如果目录中还有其他来源的站点清单，也请一并提供整个 `GNSS_list` 文件夹。

## 如果原始包中有，也请一并提供

```text
download_lables/data/geoids/egm96-5.pgm
```

这个 EGM96 文件不是当前公开 UCAR 探针的启动硬依赖，但后续 GNSS 高程修正和 PWV 计算建议保留。

## ERA5 部分

ERA5 下载脚本、MATLAB 积分代码目前已经找到；后续还需要统一：

- Windows 本地输出目录；
- ERA5 变量、空间范围和时间范围；
- Python 依赖安装环境；
- 训练截止年与未来验证年份。

这部分我们会在实验范围冻结后再处理。当前不需要您在聊天中发送任何账号、密码、密钥或 Token；如有认证配置，请只提供脱敏后的文件位置或由您在本机配置。

在这些资料补齐前，我们会先继续完成不依赖新 GNSS/ERA5 数据的模型结果复核、站点误差分析、覆盖率核查、文档整理和实验协议准备，不会把临时探针数据混入正式结果。

谢谢老师！
