# 二阶段部署架构与研究边界（2026-08-17）

> 本文只整理已存在代码和正式回放结果，不启动新数据下载，不改变模型或训练流程。凡涉及未来独立 GNSS/ERA5 数据的内容均属于待验证计划。

## 1. 研究链路

当前方案是“一阶段预测地表状态 + 二阶段廓线到 PWV 的直接映射”：

```text
GNSS ZWD + 时间/站点信息
        │
        ├─ 第一阶段 GRU + XGBoost
        │      └─ 预测地表 PS、TS、WPS（Tm 仅作诊断，不作为二阶段输入）
        │
        ├─ 气候态垂直廓线
        │      └─ 用 P1 的 PS/TS/WPS 做地表层替换/订正
        │
        └─ 第二阶段 ProfileTransformer
               └─ 输出转换系数 Pi，再由 PWV = Pi × ZWD 得到 PWV
```

二阶段的核心价值不是把第一阶段输出直接当作 PWV，而是用第一阶段预测的地表状态修正无探空部署时可获得的廓线先验，再由廓线模型完成 ZWD→PWV 的映射。

## 2. 主要比较口径

| 口径 | 含义 | 部署状态 |
|---|---|---|
| `real` | 使用真实探空廓线 | oracle 上界，不可作为部署精度 |
| `real_surf_p1` | 真实廓线，地表 PS/TS/WPS 换成 P1 预测 | 误差敏感性分析 |
| `clim` | 纯气候态廓线 | 无实时地表修正的基线 |
| `clim_surf_p1` | 气候态廓线，仅地表层用 P1 预测替换 | 当前权威部署候选 |
| `clim_adj_p1` | 气候态廓线整体订正到 P1 地表状态 | 对照方案，不能与最佳口径混用 |
| `GPT3` | 经验模型基线 | 部署比较基线 |

当前 2014–2019 严格回放的权威候选是 `clim_surf_p1`。早期 2017 单年文档中的最佳方案和样本数属于 legacy，不能与六年正式结果混合引用。

## 3. 当前正式结果能说明什么

在固定官方 36 站、2014–2019 共同样本（110,928 条）上：

- `clim_surf_p1` RMSE：0.242729 mm；
- GPT3 RMSE：0.346177 mm；
- 相对 RMSE 降低：29.88%；
- 年度有样本站点数：27–30；
- 36 个站点中 3 个站点标记为 `LOSS`：`COM00080222`、`SFM00068842`、`SAM00040417`。

这支持“在历史回放式堆叠部署口径下，第一阶段地表预测可显著改善气候态廓线驱动的二阶段结果，并总体优于 GPT3”的表述。

## 4. 必须明确的部署边界

当前评估仍然使用真实 GNSS ZWD 作为湿延迟锚点，并使用历史信息构建的 P1 特征和气候态 cache。因此它是：

```text
历史回放式堆叠部署评估
```

而不是：

```text
完全自治实时系统
```

也不是：

```text
训练截止年之后的严格未来外推验证
```

在论文、汇报和对老师说明中，必须把这三个概念分开。不能把 oracle `real` 结果写成实际部署精度，也不能把当前回放结果写成已经完成的未来独立验证。

## 5. 当前研究成果的可信层级

### 已完成、可作为正式结果

1. 第一阶段模型方案和输入预测结果整理；
2. 第二阶段 ProfileTransformer 的训练与官方 36 站结果复核；
3. 一阶段输出堆叠到二阶段的六年严格回放；
4. 全局、年度、站点级和弱站输入误差诊断；
5. GPT3 对照及结果口径审计。

### 已完成、但属于探索性/工程验证

1. UCAR 单日公开网络探针；
2. 探针站点文件、时间字段、缺测和非正 PWV 统计；
3. 下载器入口、配置依赖和失败语义的离线审查。

### 尚未完成、不能提前宣称

1. 有版本匹配站点清单的 IGS 批量下载；
2. 多年 ERA5 下载和垂直积分复核；
3. 训练截止年后的未来独立验证；
4. 真实新数据上的模型泛化结论。

## 6. 等待资料期间的工作边界

可以继续：

- 结果表、图和论文/PPT 口径统一；
- 弱站及 PS/TS/WPS 输入误差的描述性分析；
- 未来验证协议、数据字典、排除规则和结果清单冻结；
- 下载器离线接口、路径和假成功风险审查。

暂不执行：

- IGS 多年批量下载；
- 多年 ERA5 下载；
- 认证脚本；
- 未经高风险审阅的生产质量规则修改；
- 将 UCAR 探针数据混入正式六年结果。

## 7. 资料补齐后的恢复闸门

```text
老师提供非敏感配置/站点清单/静态资源路径
→ strict preflight 通过
→ 冻结 ERA5 产品、变量、区域、时间、输出路径
→ 记录依赖版本
→ 单日、单源、隔离目录试验
→ 检查文件数、时间覆盖、NetCDF 和积分结果
→ 通过后才进行批量下载和未来独立验证
```

不需要老师发送账号、密码、密钥或 Token；认证内容应由老师在其本机或受控环境配置。
