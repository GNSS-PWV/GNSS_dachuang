> ## 2026-08-17 状态校正（优先于本文旧堆叠结果）
> 老师最新的“一阶段预测内容驱动二阶段新模型”任务已完成严格六年回放验收。主结论：2014–2019 共 **110,928** 个共同样本、官方 36 站跨年度联合覆盖；最佳部署方案为 `clim_surf_p1`，RMSE **0.242729 mm**，相对 GPT3（0.346177 mm）降低 **29.88%**。年度实际覆盖为 27–30 站，不是每年 36 站全覆盖。详见 `第二阶段/2014_2019严格回放式堆叠部署评估_成果汇总_20260817.md`；本文中 2026-08-11 的单年堆叠数值只作 legacy 记录，不得混用。
>
> 口径限制：真实 GNSS ZWD 仍是湿延迟锚点，第一阶段使用历史观测滞后特征，气候态 cache 为全历史 cache；因此只能称为**历史回放式堆叠部署评估**。
# 项目交接文档（给下一阶段新对话 / 新成员）
> 生成日期：2026-08-16　|　项目：基于深度学习的两步法实时 GNSS 水汽反演（国家级大学生创新创业训练计划，同济大学）
> 团队：王乐萱、彭怡焱（Pengyiyan0411）、钱顾冲、程禹卜、李乾凯而　|　导师：吴志露
> 仓库：https://github.com/GNSS-PWV/GNSS_dachuang（master，公开）
> 本文件是**全量交接**：读完本文件 + 本地 `D:\gnss水汽反演\` 目录即可无缝接手。

---

## 0. 一句话总览
- **第一/二阶段核心科学内容全部完成**：一阶段 4 气象参数 GRU+XGB；二阶段 ProfileTransformer（PWV=Pi×ZWD，不用 Tm），官方 36 站 RMSE **0.0653→0.0609 mm**，较 GPT3 提升 ~82%。
- **第三阶段**（优化/验证/格网/部署模拟/端到端）与 **GNSS 泛化验证**（IGS 2019，武汉真值 R²=0.897）均完成。
- **本次新完成（2026-08-11，组会任务）**：**一阶段→二阶段堆叠**——用一阶段预测的地表温压订正气候态廓线喂给二阶段，验证"没有真实温度气压"的部署精度；并做了**部署鲁棒性微调**，微调后无真实数据部署 **RMSE 0.239 mm（官方36站子集）**，**击败 GPT3（0.312）约 23%**。
- **进行中**：CCF-A 冲顶可行性（PM2.5 通用框架，冷启动实验待续跑）；ERA5 作真值的 GNSS 泛化验证（等 ERA5 数据）。

---

## 1. 环境与凭据（重要）

### 1.1 服务器（计算主力，数据都在上面）
- 服务器访问：通过个人凭据管理器与学校 VPN 获取；不要将账号、地址、端口或密码写入交接文档。
- 网络故障排查：确认学校 VPN 与个人访问权限；不要在文档中记录服务器地址或备用 IP。
- **需要开同济 VPN**（用户侧控制）；最近一次连接正常（2026-08-11）
- 数据根目录：`/share/home/[REDACTED_USER]/tj23114/packages/dachuang_pwv/`
- Python 环境：由获授权用户在个人服务器会话中确认；不要将服务器账号或绝对个人目录写入公开交接材料。
- 提交作业：`sbatch`（分区 L40 / A800，脚本模板见 `phase2/run_*.sh`：`#SBATCH`、`--gres=gpu:l40:1`、`source activate kokolo`）
- 日志目录：由获授权用户在个人服务器会话中确认。

### 1.2 GitHub
- 仓库：`GNSS-PWV/GNSS_dachuang`（master）
- GitHub 认证：仅通过本机凭据管理器读取；不得把 Token 写入文件、命令历史或交接文档。
- **直接 git push 常连接重置**；可靠方式 = **GitHub REST API 上传**（api.github.com 通）：
  `PUT /repos/GNSS-PWV/GNSS_dachuang/contents/<path>`，body `{message, content=base64, branch:'master', sha(更新时)}`
- 提交身份：user `Pengyiyan0411`，email `2451299@tongji.edu.cn`（作者+提交者都要设成这个，不要用 codex）

### 1.3 本地
- 项目根目录：`D:\gnss水汽反演\`
- Python：`D:\Users\lenovo\anaconda3\envs\torch_gpu\python.exe`；**必须**先设 `KMP_DUPLICATE_LIB_OK=TRUE`
- 显卡：8GB（显存小 + **系统无页面文件，整机内存紧张，曾多次内存耗尽中断**）→ 大训练/推理一律放服务器；本地只做小脚本/文档
- ⚠️ **PowerShell heredoc 管道会损坏中文路径**（变成 `?`）：凡是涉及中文路径的 Python 操作，写成 `.py` 文件（用 `[System.IO.File]::WriteAllText(..., UTF8Encoding(false))`）再运行，或通过 argv 传路径；PowerShell 自身的 `Get-Content/Get-ChildItem` 处理中文路径没问题
- ⚠️ 递归删除被安全策略拦截：用 Python `os.remove/shutil.rmtree` 或 `Remove-Item -LiteralPath`（非递归）

### 1.4 数据位置
- 服务器训练数据：`.../dachuang_pwv/PS/xg_data/{2014..2019}/{站}_met.txt`（每行一个高度层；列：TIME,YEAR,DOY,LAT,LON,ELV,TS,PS,WPS,ZWD,ZHD,ZTD,PWV,Tm；同 TIME 多行=一条廓线，地表=最低 ELV 行）
- 本地 2019 IGS ZTD：`D:\gnss水汽反演\2019\`（SINEX TROP，每站每天 .gz）；站点坐标 `D:\gnss水汽反演\IGSwhu_formatted(1).txt`
- 本地探空真值：`第二阶段/radiosonde_2019/CHM00057494_met.txt`（武汉）等 4 站
- 格网 Pi：`第二阶段/result_grid_monthly/pi_grid_all.csv`；月/季节气候态廓线缓存：服务器 `phase2/result_grid/st_seasonal_cache.pkl`、`result_grid_monthly/st_monthly_cache.pkl`

---

## 2. 第一阶段（完成 ✅）
- 模型：GRU（LayerNorm+FC）+ XGBoost 残差/堆叠；Optuna；特征约 50 维（时间周期/站点地理/滞后/差分/交互）
- 官方精度（2017 测试年）：Ps 4.374 hPa / WPS 2.193 / Ts 3.918 K / **Tm 3.715 K（优于 GPT3 5.36K，且无系统偏差）**
- **"加 CNN 反而变差"已干净复现并解释**（200 站子集，PS）：GRU 3.868 vs CNN-BiLSTM-Attention 7.784（+101%）；原因：过参数化（175万 vs 10万）+ 卷积与单点强自相关回归任务错配 + MaxPool 丢信息 + 深层混合结构不稳定。结论：纯 GRU+XGB 成立。
- 一阶段预测输出格式：按站按天的**地表**值文件 `DOY StationID Predict True Error`（每天 00/12Z 各一条）；预测目录（服务器）：
  - PS：`PS/ps_result_2/gru/predictions/PS/year_2017/`
  - TS：`Ts_Tm/ts_result/gru/predictions/TS/year_2017/`
  - WPS：`WPS/wps_result_1/gru/predictions/year_2017/`
  - Tm：`Ts_Tm/tm_result/gru/predictions/Tm/year_2017/`
  - 模型权重：`PS/ps_result_2/gru/gru_model.pth`、`Ts_Tm/ts_result/gru/gru_model.pth`、`Ts_Tm/tm_result/gru/gru_model.pth`、`WPS/wps_result_1/gru/gru_model.pth`
- ⚠️ 一阶段预测**只有 2017 年**；如需 2014–2019 全时段堆叠，要用上述权重对全时段重新预测（未做，待办）

## 3. 第二阶段（完成 ✅）
- 模型：ProfileTransformer——输入廓线 `[ELV,TS,PS,WPS]` + 连续高度位置编码 + 全局特征 `[ZWD,lat/lon,DOY/hour]`；输出 Pi（sigmoid 限幅 0.05–0.35）；`PWV=Pi×ZWD`，**不用 Tm**；参数量 85.3 万
- 官方 36 测试站（N=113,294）：**RMSE 0.0653 → Optuna 后 0.0609 mm**（R²=1.0000，Bias +0.008）
- 对比：Saastamoinen 全流程 7.13 / 真值 Tm 公式 0.255 / GPT3 0.345 / **Transformer 0.065**
- 分层：冬 0.045/夏 0.079；极地 0.041/热带 0.116；ZWD<50→0.027、>200→0.082；极端场景（湿区/强变化）较 GPT3 提升 75–82%
- 格网 Pi 产品：5°×5°×12月（Pi 极地 0.147→热带 0.166，夏季高于冬季，符合 Tm 气候学）
- 部署模拟：真实廓线 0.065 vs 气候态平均廓线 0.965
- 端到端两步法（2017）：Transformer 0.055 < 传统两步法(GRU-Tm) 0.224 < GPT3 0.332
- 误差订正：逐站留一订正 0.065→0.053
- 消融（200 站子集，PWV RMSE）：baseline 0.097；去廓线 28.8（**廓线决定性 ~297×**）；去全局 0.143（+47%）；直接预测 PWV 0.473（Pi 参数化更好）；层数不敏感 0.094/0.096
- 层贡献诊断：近地面 2km 主导 Pi（冬季 1.5%、暴雨 6%），2–4km 暴雨时反向（−1.75%）
- **物理一致性正则（phys_lambda=1.0）**：RMSE 0.097，与基线持平（无提升）——`result_ablation/phys1/`
- 代码/模型/scalers：`第二阶段/model.py, train.py, data.py`；模型 `第二阶段/result_aligned_official/best_model.pth + scalers.pkl`（本地可离线推理）

## 4. GNSS 泛化验证（完成 ✅）
- 管线 `第二阶段/gnss_generalization.py`：IGS 2019 ZTD → ZHD(GPT3气压+Saastamoinen) → ZWD → **Pi(月尺度格网查表)** → PWV
- 9 中国站：拉萨 PWV~10 / 香港~43 / 武汉峰值 84.3 mm（物理合理）；暴雨个例武汉 2019-06-20 峰值 83.5、香港 07-02 76.5
- 武汉真值对比（wuh2 vs 探空 CHM00057494，2019）：端到端 RMSE 5.78 / R²=0.897；分时次 00Z 4.16 / 12Z 4.95；事件期 00Z 4.16 R² 0.946
- 多站真值对比（4 站 N=2030）：ours 3.59 vs GPT3 3.63（部署口径 +1%，诚实呈现）
- 产物：`第二阶段/gnss_pwv_*.csv`、`gnss_pwv_timeseries.png`、`extreme_*.png`

## 5. CCF-A 冲顶可行性（进行中 ⏳）
- 动机：把 PWV 方法论抽象为"**通用非均匀地理时序 + 物理锚点重参数化**"，先在公开多站点数据验证通用性，再决定是否投入冲顶
- 第一基准：Mendeley「Air quality dataset」DOI `10.17632/p86hnykz3d.2`（斯洛文尼亚 21 站逐小时 PM10/PM2.5+气象，2024-05~2025-12，每站 14100 条；坐标从 ARSO 官方 XML 拿）
- 本地目录：`D:\gnss水汽反演\通用框架_CCFA\`（data/ src/ results/ + `CCF-A_冲顶可行性验证.md`）
- 协议：站点级留出（14 训练/3 验证/4 测试，未见站零样本）；三支柱：连续坐标 PE / 全局条件 / 锚点重参数化（y=anchor×exp(g)）
- 结果（seed42，未见站 RMSE）：
  | 模型 | 1h | 6h | 24h |
  |---|---|---|---|
  | ours_clim_mul（空间+锚点） | 6.95 | **9.01** | 10.24 |
  | gru_anchor_clim（仅锚点） | 6.82 | 9.21 | **9.63** |
  | ridge | 6.60 | 10.28 | 10.54 |
  | persistence | **6.16** | 13.93 | 12.05 |
  | 纯 GRU（未见站） | 15.49 | 15.56 | 15.61 |
- 结论（部分成立）：**物理锚点=跨站泛化关键**（纯 GRU R²≈0.05→+锚点 R² 0.64–0.82）；**6h/24h 预报击败 ridge 与 persistence**；1h 被 persistence 压制；**空间索引单独无效、叠加无增益（待冷启动确认）**
- **冷启动实验**（`通用框架_CCFA/src/cold_start.py`，无本地历史只靠坐标+时间）**8/8 被系统中断未完成**——这是空间索引支柱的决定性检验，**建议续跑**（本地 ~10 分钟，注意内存）
- 后续扩展（若成立）：Beijing/China PM2.5、METR-LA/PEMS-BAY 交通、NOAA 气象站网（3+ 基准 SOTA）+ 理论（收敛性/复杂度下界）

## 6. 一阶段→二阶段堆叠（本次组会任务，完成 ✅）
> 组会结论：把一阶段模型完善后堆叠到二阶段，考察"没有真实温度气压"的表现；真值验证用站点+ERA5。

### 6.1 脚本（本地 `第二阶段/`，已上传服务器 `phase2/`）
- `phase2_p1_deploy.py`：6 口径部署对比（real / real_surf_p1 / clim / clim_surf_p1 / clim_adj_p1 / gpt3），2017 年
- `analyze_p1deploy.py`：全站 + 官方36测试站子集出表 + 图
- `p1deploy_finetune.py`：部署鲁棒性微调（气候态廓线+一阶段误差噪声，p_deploy=0.5，微调自 result_aligned）

### 6.2 结果（2017；全站 N=172,672/261 站；官方36子集 N=15,723/25 站）

**未微调（result_aligned 直接推理）— 官方36子集：**
| 口径 | RMSE (mm) |
|---|---:|
| real（oracle，真实廓线） | 0.0757 |
| real_surf_p1（真实廓线+一阶段预测地表温压） | 0.0840（+11%）|
| clim（纯气候态廓线） | 2.1552 |
| clim_surf_p1（气候态+一阶段预测地表） | 0.3860 |
| **clim_adj_p1（气候态整体订正到一阶段地表）** | **0.3331** |
| GPT3 | 0.3120 |

**微调后（result_p1deploy_ft）— 官方36子集：**
| 口径 | RMSE (mm) | R² | Bias |
|---|---:|---:|---:|
| real | 0.0835 | 0.99997 | +0.011 |
| real_surf_p1 | 0.0915 | 0.99997 | +0.007 |
| clim | 0.4819 | 0.99911 | −0.147 |
| **clim_surf_p1** | **0.2413** | 0.99978 | −0.022 |
| **clim_adj_p1** | **0.2389** | 0.99978 | +0.032 |
| GPT3 | 0.3120 | 0.99963 | −0.161 |

**微调后全站（N=172,672）：clim_adj_p1 0.2170 / clim_surf_p1 0.2179 / GPT3 0.3245 / real 0.0624**

### 6.3 结论（可写进论文/汇报）
1. **oracle 场景**：真实廓线时地表温压换成一阶段预测只退化 +11%——二阶段对地表温压误差不敏感。
2. **"传统模型直接映射到新模型"成立**：一阶段预测地表温压把"纯气候态部署"从 2.16mm 压到 0.33mm（−85%），追平 GPT3。
3. **部署鲁棒性微调再压 28%**：0.333→0.239（官方36子集），**反超 GPT3 约 23%**，且偏差更小（+0.032 vs −0.161）；纯气候态（不订正）也 2.16→0.48。
4. 剩余差距（0.239 vs oracle 0.083）来自气候态廓线上层结构与真实廓线的差异。
- 产物：服务器 `phase2/result_p1deploy/`（未微调）与 `result_p1deploy_ft_eval/`（微调后）；本地 `第二阶段/result_p1deploy/`、`第二阶段/result_p1deploy_ft_eval/`；文档 `第二阶段/一阶段堆叠二阶段_部署对比.md`

## 7. ERA5 作真值的 GNSS 泛化验证（待办 ⏳ 差数据）
- 目标：以 ERA5 积分 PWV 为真值，在 GNSS 站点观测上验证我们反演 PWV（部署口径：格网 Pi + GPT3 气压）的泛化精度；分站点/时次/季节报告 RMSE/MAE/R²/Bias
- **GNSS 侧已就绪**：`第二阶段/gnss_generalization.py`（9+ 中国站已出）
- **卡点**：ERA5 积分 PWV 数据**老师还没给**（老师说在弄）；服务器上只有另一项目（FY-4B/puyi_data）的 ERA5 patch，不是我们要的
- 两条路：① 老师提供站点匹配好的 ERA5 PWV（最快）；② 自己从 ECMWF CDS 下载（需 CDS 账号，下载+积分+插值约半天~1天）

## 8. 服务器当前状态（2026-08-11 最后确认）
- 队列无作业；`result_ablation/phys1/`（物理一致性，RMSE 0.097，与基线持平）已完成
- `result_p1deploy/`、`result_p1deploy_ft/`（best_model.pth + scalers.pkl）、`result_p1deploy_ft_eval/` 都在服务器
- 本地已同步上述结果到 `第二阶段/`；**CCF-A 与堆叠的新脚本/结果尚未推 GitHub**（待办）

## 9. 待办清单（按优先级）
1. **冷启动实验续跑**（`通用框架_CCFA/src/cold_start.py`，本地 ~10 分钟）→ 判定空间索引支柱 → CCF-A 去留
2. **一阶段全时段预测**（用服务器 4 个 gru_model.pth 对 2014–2019 预测）→ 堆叠验证覆盖二阶段官方全测试集（现在只有 2017）
3. **GitHub 同步**（REST API）：`phase2_p1_deploy.py`、`analyze_p1deploy.py`、`p1deploy_finetune.py`、`result_p1deploy*` 结果、`一阶段堆叠二阶段_部署对比.md`、`通用框架_CCFA/`、本文档
4. **ERA5 验证**：向老师要 ERA5 积分 PWV（或自下 CDS）；到位后写 `era5_validation.py`（复用 gnss_generalization 管线）出对比
5. **论文线**：素材已齐（对比/分层/时序/格网/GNSS 泛化/真值对比/堆叠），可投 RSE/ACP；或按 CCF-A 结果决定是否冲顶
6. 上海暴雨 GNSS ZTD（老师解算后接入）、临港（2 周后精密轨道）

## 10. 给新对话的注意事项
- 先读：`项目完整总结.md`（8/7 全量）、`项目当前情况与下一阶段交接.md`（8/7）、`通用框架_CCFA/CCF-A_冲顶可行性验证.md`（8/8）、`第二阶段/一阶段堆叠二阶段_部署对比.md`（8/11）、本文件
- 服务器连不上=VPN 没开（需用户侧）；GitHub API 通常可用
- 本地大实验会内存耗尽，优先服务器 sbatch
- 修改 Python 文件里的中文路径时，不要用 `Get-Content`+`WriteAllLines` 回写（会乱码），用 here-string `WriteAllText(UTF8)` 整体重写
---

## 11. 2026-08-16 续跑更新：CCF-A 冷启动首轮结论

- `通用框架_CCFA/src/cold_start.py` 的 seed=42 实验已完整结束；结果位于 `通用框架_CCFA/results/coldstart_seed42/summary.json`。
- 为保证基线公平性，已修正无锚点直接回归路径的错误输出限幅，并把 Ridge 改成低内存的 `float32 + lsqr` 实现。
- 首轮严格冷启动（仅坐标+时间，未见站）结果：`mlp_plain` **RMSE 12.910 / R² 0.345**，`mlp_spatial` **13.100 / 0.325**，`mlp_spatial_clim` **13.561 / 0.277**。空间坐标嵌入没有增益。
- 因此 CCF-A 原型暂为**不支持扩大投入**；下一步仅做 1–2 个 seed 的低成本稳健性复核。若仍无空间增益，转回 PWV 的 RSE/ACP 论文线，而非继续扩展 CCF-A 数据集。