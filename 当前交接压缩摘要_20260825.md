# 当前交接压缩摘要（2026-08-25）

## 已完成

- 已按 `D:\myai\AGENTS.md` 执行；不读取凭据、不运行 UCAR 授权、不把实验性标签冒充正式结果。
- IGRA 统一锁定 2026-08-23 目录版本，忽略后续 NCEI 文件大小变化。
- 365 个目标站点完成最终 ZIP 审计：232 个手动来源 CRC 有效，131 个批量来源 CRC 有效，合计 363 个有效；`ICM00004018`、`POM00008579` 无目录匹配。
- 手动目录的 232 个有效 ZIP 已同步到 `igra_batch_20260824`。
- 公开站点表已下载：`第二阶段\igra2-station-list_20260825.txt`。
- 目标元数据对齐表：`第二阶段\igra_target_metadata_20260825.csv`。
- 审计脚本和结果：
  - `第二阶段\audit_igra_20260823_final.py`
  - `igra_batch_20260824\final_audit_20260823.csv/.json/.txt`
- profile 批处理入口已准备：`第二阶段\build_igra_profile_batch.py`，默认定位为实验性重建，不直接启动正式训练。

## 当前阻塞

- 363 个有效 ZIP 已全部解析为实验性 `*_met.txt` profile；这些不是老师确认的正式标签。
- 13 个站点转换后只有表头、无可接受 profile；2 个无 IGRA 匹配站点不应虚构。
- EC 数据尚未提供。
- ERA5 真实 NetCDF/积分链路尚未完成最小验收。
- 默认 Python 无 PyTorch，但 `D:\Users\lenovo\anaconda3\envs\torch_gpu` 已确认可用：PyTorch 2.7.0+cu126、CUDA 和 RTX 4060 均正常；同济服务器连接目标未配置，不能安全猜测上传地址。

## 严格切分规则

- 实际有 2014–2018 profile 的核心站点为 350；seed=42；留站点数 `floor(350*0.1)=35`。
- 留站点 2014–2018 全部进入 `val_leave_station`。
- 其余站点：2014–2016 train，2017 test，2018 val。
- 2019 全部进入独立 `val_2019`。
- Scaler 只在 train 拟合。

## 下一步

1. 由老师确认实验性 IGRA 重建标签是否允许用于训练，或提供正式 `*_met.txt`/EC 标签。
2. 若确认可用，再将 `result_strict_igra_reconstructed_20260825` 作为实验输入，先做小规模 dry-run。
3. 资料闭环后，由用户提供同济服务器连接目标，再上传代码、manifest 和冻结数据。
4. 通过 GPU 分区 `sbatch` 做 dry-run，再提交正式重训和五集合评估。

## 最新本地结果

- 363 个 CRC 有效 ZIP 已全部完成实验性 profile 重建；输出目录：
  `第二阶段\profile_reconstructed_igra_20260825`。
- 全量预检：`PASS`，字段错误 0，profile 数 1,310,588，五个集合均非空。
- 严格切分目录：`第二阶段\result_strict_igra_reconstructed_20260825`。
- 切分计数：train 584,904；test_2017 198,654；val_2018 198,262；
  val_leave_station 110,230；val_2019 218,538。
- 实际 2014–2018 有可接受 profile 的站点为 350，因此留站点为
  `floor(350*0.1)=35`。这是实际可用站点口径，不人为补到 36。
- 13 个站点 ZIP 有效但转换后只有表头、没有可接受 profile；另有 2 个站点无 IGRA 目录匹配。
- 这些 PWV/ZWD 是 IGRA 水汽积分和物理 Tm 重建标签，属于实验性数据，不等同于老师正式标签。

## 预计耗时

- 无需继续下载大文件。
- 363 站解析与重建：已完成，约 5.7 GB 输出。
- 上传：约 0.5–3 小时，取决于服务器链路和是否上传原始 ZIP。
- GPU 重训：约 3–6 小时，需以 dry-run 实测为准。
