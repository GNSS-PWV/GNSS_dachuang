# 基于深度学习的两步法 GNSS 水汽反演：通俗完整成果介绍

> **版本：2026-08-17**  
> **适用对象：** 指导老师、答辩评委、项目成员，以及希望快速理解本项目的人。  
> **阅读提示：** 本文同时解释“做了什么、为什么这样做、模型如何工作、结果意味着什么、还能怎样改进”。文中所有部署类主结论均以 2014–2019 严格回放评估为准；早期单年实验仅用于说明研究演进。

---

## 一、用一句话说清我们做成了什么

我们构建了一条由两个深度学习阶段组成的 GNSS 水汽反演路线：

1. **第一阶段**用 GRU + XGBoost 预测站点地表气象状态；
2. **第二阶段**用 ProfileTransformer 读取“温度、气压、水汽压随高度变化的廓线形状”，学习把 GNSS 湿延迟（ZWD）转换为可降水量（PWV）的非线性规律；
3. 在没有实时真实探空廓线时，再由第一阶段预测的地表 **PS / TS / WPS** 改善气候态廓线，使第二阶段能够进行历史回放式部署评估。

在最严格、最接近部署问题的六年评估中，最佳可部署方案为 **`clim_surf_p1`**：

- 2014–2019 六年共 **110,928** 个共同样本；
- 覆盖官方 36 站的跨年度联合并集；
- PWV RMSE 为 **0.242729 mm**；
- 相比 GPT3 基线的 **0.346177 mm**，RMSE 降低 **29.88%**；
- 36 个站中有 **33 个站**的站点 RMSE 低于 GPT3。

这说明：**第一阶段预测的地表气象信息，能够有效给第二阶段的新模型提供部署先验；而第二阶段直接学习垂直大气结构，能够显著优于依赖经验加权平均温度的传统 GPT3 路线。**

---

## 二、这个问题为什么重要？

### 2.1 PWV 是什么

PWV（Precipitable Water Vapor，可降水量）可以通俗理解为：把某地上空整根大气柱中的水汽全部凝结成液态水，平铺在地面上会有多厚，单位通常是毫米。它与暴雨预报、数值天气预报同化、气候监测、雷暴对流研究和高精度定位都有关。

GNSS 卫星信号穿过大气会被延迟，其中由水汽造成的部分叫 **ZWD（Zenith Wet Delay，天顶湿延迟）**。ZWD 与 PWV 强相关，因此 GNSS 可以成为连续、全天候的水汽观测手段。

### 2.2 传统做法的瓶颈

传统链条通常是：

```text
GNSS 观测 → ZWD → 估计加权平均温度 Tm → 经验公式 → PWV
```

问题在于，Tm 只是把复杂三维大气压缩成一个标量。不同地点、不同季节、不同天气形势下，即使 Tm 接近，温度、气压和水汽压的垂直分布也可能明显不同。因此，单一线性公式容易留下随地点和天气变化的系统残差。

本项目的核心思想是：**不再只问“大气平均有多暖”，而是让模型直接看“整根大气柱长什么样”。**

---

## 三、总体技术路线：两个阶段各自解决什么问题？

```text
历史地表气象观测
        │
        ▼
第一阶段：GRU + XGBoost
预测 PS / TS / WPS / Tm
        │
        │  部署时真正传给第二阶段的是 PS / TS / WPS
        ▼
气候态垂直廓线 + 预测地表状态
        │
        ▼
第二阶段：ProfileTransformer
学习 Pi = PWV / ZWD
        │
        ▼
PWV = Pi × ZWD
```

- **PS**：地表气压；
- **TS**：地表温度；
- **WPS**：地表水汽压；
- **Tm**：加权平均温度；
- **Pi**：ZWD 到 PWV 的转换系数；
- **ZWD**：GNSS 提供的湿延迟锚点。

这里需要特别澄清：当前第二阶段的正式输入是 **PS / TS / WPS**，而不是 Tm。第一阶段虽然也预测 Tm，但它目前只用于辅助诊断，**不进入 ProfileTransformer，也不决定二阶段样本能否使用**。

---

## 四、第一阶段：为什么选择 GRU + XGBoost？

### 4.1 第一阶段的任务

第一阶段不是直接预测 PWV，而是预测站点地表状态，为第二阶段没有实时探空廓线时提供可用的“地表边界条件”。它预测四个量：PS、TS、WPS、Tm。

输入特征包括：

- 时间周期信息，例如年内日期和时刻；
- 站点经纬度和高程；
- 过去若干时刻的滞后值；
- 差分、滑动统计量和变量交互项。

这些信息反映了地表气象量“变化连续、具有记忆性、又受到季节和地点约束”的特点。

### 4.2 模型结构与分工

- **GRU** 是主模型。它擅长从时间序列中保存“最近发生了什么”和“此前趋势如何”的记忆。
- **XGBoost** 用于残差/堆叠修正。直观地说，GRU 先做主要预测，XGBoost 再学习 GRU 容易漏掉的非线性小误差。

这种组合比把大量卷积、双向循环、注意力和深层全连接全部堆在一起更合适。地表气象序列通常平滑且自相关很强，最重要的信息往往就是最近值与缓慢趋势；复杂 CNN 结构反而可能池化掉关键时间细节并引入过拟合。

### 4.3 第一阶段主要结果

2017 年官方测试口径下的原始尺度结果为：

| 参数 | RMSE | MAE | R² | 单位 |
|---|---:|---:|---:|---|
| PS | 4.374 | 2.870 | 0.9969 | hPa |
| WPS | 2.193 | 1.494 | 0.9197 | hPa |
| TS | 3.918 | 2.545 | 0.9085 | K |
| Tm | 3.715 | 2.607 | 0.8963 | K |

在一个可复现的 200 站 PS 对比实验中：

| 模型 | RMSE（hPa） | 相对纯 GRU |
|---|---:|---:|
| GRU | 3.868 | 基准 |
| CNN1D | 4.151 | +7.3% |
| CNN-BiLSTM-Attention | 7.784 | +101% |
| GRU + XGBoost | 3.828 | −1.0% |

**结论：** 对本项目的平滑单点气象时间序列，GRU 的时序记忆与 XGBoost 的残差修正形成了更简洁、更稳定的组合；“网络越复杂越好”并不成立。

---

## 五、第二阶段：ProfileTransformer 到底怎么工作？

### 5.1 它不是直接背答案，而是在学习 Pi

第二阶段不直接把输入映射成 PWV，而是先预测：

```text
Pi = PWV / ZWD
PWV = Pi × ZWD
```

这样做有两个好处：

1. ZWD 的物理量级和当天观测强度被显式保留；
2. 网络重点学习“在当前大气结构下，每单位湿延迟应该折算成多少 PWV”，问题更稳定、更有物理意义。

模型输出的 Pi 通过 sigmoid 被限制在 **0.05–0.35** 的合理范围内，避免出现显然不合理的值。

### 5.2 输入：一根“竖着的大气柱”

每个时刻的探空观测按照高度排序，构成一条变长序列。每一层有四个特征：

```text
[ELV, TS, PS, WPS]
```

即高度、温度、气压和水汽压。不同探空廓线高度层数不同，模型会进行补齐并使用掩码，确保填充层不会参与注意力计算。

此外还有 9 个全局特征：

```text
ZWD、纬度的 sin/cos、经度的 sin/cos、年内日期的 sin/cos、时刻的 sin/cos
```

把经纬度、日期和时刻转为 sin/cos，是为了避免“359° 与 0° 看起来很远”“12 月 31 日与 1 月 1 日看起来很远”这样的周期边界问题。

### 5.3 架构拆解

默认基础架构为 **d_model=128、4 个 Transformer 编码层、8 个注意力头、前馈层宽度 512、dropout=0.1**，约 **85.3 万个可训练参数**。优化实验中也测试过更深、更宽的配置。

模型可以分成六步：

1. **层特征投影**：把每一层的 4 个变量线性映射到 128 维，并做 LayerNorm；
2. **连续高度位置编码**：对归一化后的实际高度使用多频率正弦/余弦编码，而不是仅使用“第几层”的编号；
3. **CLS 汇聚标记**：在廓线前加入一个可学习 CLS token，最终让它汇集全条廓线的信息；
4. **Transformer 自注意力编码器**：使用 Pre-LayerNorm、GELU、多头注意力和变长掩码，捕捉低层湿度、温度层结、中上层结构之间的关系；
5. **全局信息融合**：把 CLS 输出与 ZWD、地点、季节、时刻等全局信息的投影拼接；
6. **MLP 输出头**：经过两层 GELU 非线性输出 Pi，再与 ZWD 相乘得到 PWV。

通俗地说，传统方法像是“只量一个人体平均体温后判断身体状态”；ProfileTransformer 则像是同时看人体从脚到头的多项体征曲线，并结合所在地点、季节和当天延迟观测来判断水汽含量。

### 5.4 为什么“连续高度编码”是关键设计

不同站点、不同探空资料的高度层数和层间距并不完全一致。如果简单把“第 10 层”看成同一个位置，模型容易混淆。我们使用实际高度的连续编码，让模型知道某一层是在近地面、对流层中部还是更高处，因此更适合变长、不等间距的真实廓线。

---

## 六、训练与验证是怎样保证可靠性的？

### 6.1 数据组织

- 使用 2014–2019 年的全球探空廓线资料；资料清单共记录 **372 个唯一站点、1,206,363 条廓线**。一次全量留站实验中，以 335 个站训练、37 个未见站测试；这与后文“官方 36 站对齐实验”是不同但互补的验证口径；
- 同一时间的多行高度层构成一个训练样本；
- 只有具备至少两个有效高度层、且 ZWD/PWV 等核心量有效的样本才进入训练；
- 每条样本按高度从低到高排序，最低层作为地表层；
- 层特征、高度和全局特征均使用训练集 scaler 标准化，避免把测试集统计量泄漏给模型。

### 6.2 切分原则

第二阶段按**站点**划分训练、验证和测试，而不是简单把同一站的相邻时刻随机分到不同集合。这样测试站在训练阶段没有被模型见过，更接近“换一个地点还能否工作”的真实泛化问题。

官方对齐实验将第一阶段的官方 36 个测试站作为二阶段对应测试对象，保证两阶段比较的站点集合一致。

### 6.3 优化策略

基础训练使用：

- AdamW 优化器；
- 余弦退火学习率；
- SmoothL1（Huber）损失：小误差时像均方误差一样精细，大误差时不容易被极端样本拉偏；
- 梯度裁剪，防止异常梯度；
- 验证集 RMSE 早停，并保存最优 checkpoint 与 scaler。

服务器全量基础训练配置包括最大廓线长度 30、batch size 128、最多 100 个 epoch、patience 20。Optuna 搜索得到的一个优化配置为：d_model=128、5 层、16 头、前馈层 1024、dropout≈0.0506、学习率≈3.051e-4，得到 0.060911 mm 的官方 36 站测试 RMSE。

### 6.4 部署鲁棒性微调

纯粹用真实探空剖面训练的模型，在部署时突然换成气候态廓线会出现“训练—部署分布不一致”。因此又进行了部署鲁棒性微调：训练时以一定概率用按季、空间留站的近邻资料构建的气候态剖面代替真实剖面，并将地表层替换为“真实地表状态 + 按第一阶段误差尺度加入的随机扰动”，让模型学会在“剖面不完美”的情况下仍然利用 ZWD 和地表信息工作。

默认微调设置为 `p_deploy=0.5`、近邻数 `k=5`、15 个 epoch、学习率 `3e-5`、batch size 128、patience 5；验证集则使用纯部署式输入（`p_deploy=1`）。这一步是后续严格六年堆叠部署结果能够稳定优于 GPT3 的关键工程环节。

---

## 七、第二阶段的核心科学结果：真实廓线条件下有多准？

### 7.1 官方 36 站基准对比

在官方 36 测试站、113,294 个样本的对齐评估中：

| 方法 | RMSE（mm） | MAE（mm） | Bias（mm） | 解释 |
|---|---:|---:|---:|---|
| Saastamoinen 全流程 | 7.127 | 4.944 | −1.026 | 传统完整链条，误差主要受经验气象参数影响 |
| Saastamoinen Pi-only（真值 Tm） | 0.255 | 0.155 | +0.098 | 给传统公式真实 Tm 后的上限参考 |
| GPT3（经验 Tm） | 0.345 | 0.249 | −0.183 | 经验模型基线 |
| ProfileTransformer 基础模型 | **0.0653** | **0.0370** | **+0.0077** | 不使用 Tm，直接学习廓线到 Pi |

经过超参数优化的同站点测试结果为：

| 模型 | RMSE（mm） | MAE（mm） | Bias（mm） |
|---|---:|---:|---:|
| Optuna 优化后的 ProfileTransformer | **0.060911** | **0.036566** | **+0.001845** |

这些结果说明，在**真实垂直廓线可用**的科学验证条件下，直接学习廓线结构的模型明显优于基于经验 Tm 的 GPT3，也优于即使给了真值 Tm 的传统线性 Pi 公式。

### 7.2 早期独立留站结果为何不同

项目中还存在一个 37 个未见站、112,909 样本的全量留站结果，ProfileTransformer RMSE 为 0.0545 mm。它反映了另一次具体数据划分和模型训练运行，不能与官方 36 站 0.0653 mm 或超参数优化的 0.060911 mm 混为同一个统计口径。

因此，正式汇报时应明确写出“所用站点集合、样本数和实验条件”，而不是只挑最小的一个数字。

---

## 八、最重要的新成果：一阶段如何真正驱动二阶段？

### 8.1 为什么不能把真实探空剖面直接搬到部署现场

真实探空廓线精度很高，但不是每个 GNSS 站都能持续获得。因此要想部署，必须回答：

> 当站点只有 GNSS 的 ZWD 和可预测的地表气象状态时，第二阶段还能否工作？

我们用空间留站的气候态廓线作为“没有实时探空时的垂直结构先验”，再用第一阶段预测的地表 PS / TS / WPS 更新它。

### 8.2 六种方案分别意味着什么

| 方案 | 使用的剖面 | 地表状态 | 是否属于部署主张 |
|---|---|---|---|
| `real` | 真实探空剖面 | 真实 | 否，oracle 上界 |
| `real_surf_p1` | 真实探空剖面 | 第一阶段预测 | 否，oracle 敏感性对照 |
| `clim` | 气候态剖面 | 气候态 | 是，但没有 P1 修正 |
| `clim_surf_p1` | 气候态剖面 | **P1 预测 PS/TS/WPS 替换地表层** | **是，主方案** |
| `clim_adj_p1` | 气候态剖面 | P1 对全廓线作整体校正 | 是，比较方案 |
| `gpt3` | GPT3 经验模型 | GPT3 Tm | 是，基线 |

`real` 与 `real_surf_p1` 的意义是说明“若真实剖面已知，地表预测误差对模型有多大影响”；它们不能作为真实部署精度宣传。

### 8.3 严格六年主结果

本轮正式评估使用 2014–2019 全部六年，每年都重新使用第一阶段预测文件，并将 P1 与 P2 按 `(station, TIME)` 精确对齐。六年指标由原始样本直接合并计算（micro-average），不是把六个年度 RMSE 做平均。

| 方案 | RMSE（mm） | MAE（mm） | Bias（mm） |
|---|---:|---:|---:|
| real（oracle） | 0.073689 | 0.044886 | +0.009009 |
| real_surf_p1（oracle） | 0.079574 | 0.047874 | +0.005229 |
| clim | 0.474041 | 0.321933 | −0.178649 |
| **clim_surf_p1** | **0.242729** | **0.168026** | **−0.024330** |
| clim_adj_p1 | 0.258172 | 0.175935 | +0.043305 |
| GPT3 | 0.346177 | 0.249985 | −0.183231 |

最关键的比较是：

```text
GPT3：          0.346177 mm
clim_surf_p1：  0.242729 mm
绝对降低：      0.103448 mm
相对降低：      29.88%
```

`clim_surf_p1` 在 2014、2015、2016、2017、2018、2019 六个年度均优于 GPT3，表明改善不是某一年偶然拉出来的。站点宏平均 RMSE 也从 GPT3 的 0.322701 mm 降至 0.229419 mm。

![年度 RMSE 对比](第二阶段/result_p1deploy_ft_strict_20260817/analysis_2014_2019/strict_annual_rmse_2014_2019.png)

![年度样本与站点覆盖](第二阶段/result_p1deploy_ft_strict_20260817/analysis_2014_2019/strict_coverage_2014_2019.png)

### 8.4 一个值得解释的发现：为什么“只替换地表”反而更好？

直觉上，把整条气候态廓线都校正到第一阶段预测的地表值似乎应该更强；但严格六年结果中，`clim_surf_p1`（0.242729 mm）优于 `clim_adj_p1`（0.258172 mm）。

目前合理的解释是：第一阶段预测最可靠的是**地表状态**。如果把地表误差以比例或平移方式传递到高层，可能会破坏原本较合理的气候态垂直结构；只替换地表层则保留了中高层的气候态形状，同时提供最直接、最可信的边界条件。这个解释是依据结果提出的研究假设，后续还应通过分高度、分季节和分气候区消融实验进一步验证。

---

## 九、严格性与可追溯性：我们如何避免“看起来很好、其实不严谨”？

本轮正式部署评估增加了以下保护：

1. **TIME 精确匹配**：新版 P1 文件含 `TIME` 时，只允许 `(station, TIME)` 精确对齐；不把同一 DOY 的不同时刻混在一起；
2. **不使用未来目标分布筛样**：正式 P1 推理使用 `outlier_policy = none`；
3. **Tm 不干扰样本资格**：PS / TS / WPS 决定二阶段是否有完整输入，Tm 缺失只做诊断；
4. **气候态均值修正**：按高度、按变量以 sum/count 等权汇总，而不是递推平均；
5. **逐年审计**：保存精确匹配、fallback、未匹配、每站覆盖以及 checkpoint/scaler/cache/GPT3 格网的哈希摘要；
6. **共同样本比较**：六种方法只在同一批有效样本上比较；
7. **重复键检查**：每年部署 CSV 的 `(station, time)` 无重复。

审计事实是：PS / TS / WPS / Tm 在六年中 **DOY fallback 均为 0**。2014–2018 年仍有少量时刻没有精确 P1 预测，这些时刻被剔除，没有被“回退匹配”或填补；最终保留的 110,928 条记录对六个方案完全一致。

---

## 十、我们现在能怎样表述成果？

### 可以正式使用的表述

> 本项目提出由 GRU+XGBoost 地表状态预测与 ProfileTransformer 垂直廓线建模组成的两阶段 GNSS 水汽反演框架。第二阶段不依赖 Tm 线性公式，而是学习温度、气压和水汽压垂直结构到 ZWD–PWV 转换系数的非线性映射。在 2014–2019 六年、110,928 个共同样本、官方 36 站跨年度联合覆盖的严格历史回放式堆叠部署评估中，采用第一阶段预测 PS/TS/WPS 替换气候态剖面地表状态的方案取得 0.242729 mm 的 PWV RMSE，较 GPT3 的 0.346177 mm 降低 29.88%。

### 不应使用的表述

- “已经实现完全无真实数据的实时自主部署”；
- “每一年都在 36 个站上验证”；
- “`real` oracle 结果就是实际部署精度”；
- “本轮已严格证明未来年份外推能力”；
- 把旧 2017 单年堆叠指标和本轮六年严格结果混在一张表里。

---

## 十一、必须诚实说明的边界与下一步

### 11.1 当前边界

1. **真实 ZWD 仍被使用**：它是 GNSS 观测提供的湿延迟锚点，因此本实验不是端到端地从原始 GNSS 相位数据开始解算；
2. **第一阶段是历史回放预测**：特征含历史观测滞后信息，尚未做完全递归的自治输入实验；
3. **气候态 cache 使用全历史资料**：目前能称为空间留站、全历史气候态的回放模拟，不能称为严格“训练截止年后预测未来年”；
4. **年度可用站数为 27–30**：六年联合并集才是 36 站；
5. **真实廓线结果与部署结果服务于不同问题**：0.060911 mm 说明模型学习能力，0.242729 mm 才是当前可部署口径的主结果。

### 11.2 下一步最有价值的工作

1. 做分季节、纬度带、气候区和站点的误差分析，解释 3 个未优于 GPT3 的站点；
2. 分高度层分析 `clim_surf_p1` 优于 `clim_adj_p1` 的原因；
3. 构造“训练截止年—未来年份”实验，并让第一阶段采用递归/预报可得输入，验证真正未来部署能力；
4. 对接 ERA5 或数值天气预报剖面，替代全历史气候态，进一步缩小部署与真实剖面之间的差距；
5. 将严格六年表、年度图、典型站时间序列和审计说明整理为论文与答辩材料。

---

## 十二、快速术语表

| 术语 | 通俗解释 |
|---|---|
| GNSS | 卫星导航系统，可从信号传播延迟中提取大气信息 |
| ZWD | 卫星信号被水汽造成的天顶方向延迟 |
| PWV | 整层大气水汽折算成液态水后的厚度 |
| Tm | 加权平均温度，传统经验公式常用但信息压缩严重 |
| Pi | ZWD 转 PWV 的转换系数，Pi = PWV / ZWD |
| 探空廓线 | 温度、气压、水汽压等随高度变化的一条竖直曲线 |
| 气候态廓线 | 由历史资料平均得到的典型垂直结构 |
| oracle | 使用现实部署中不可获得的真值信息得到的理论上界对照 |
| micro-average | 把所有样本合并后统一算指标，样本多的站权重更大 |
| 站点宏平均 | 先分别计算每站指标，再对站点等权平均 |

---

## 十三、可追溯材料

- 严格六年正式结果：`第二阶段/result_p1deploy_ft_strict_20260817/analysis_2014_2019/`
- 严格部署成果与限制：`第二阶段/2014_2019严格回放式堆叠部署评估_成果汇总_20260817.md`
- 运行/审计状态：`第二阶段/strict_run_live_status_20260817.md`
- 一阶段到二阶段的历史结果说明：`第二阶段/一阶段堆叠二阶段_部署对比.md`
- 模型源码：`第二阶段/model.py`、`第二阶段/data.py`、`第二阶段/train.py`
- 第一阶段模型比较：`第一阶段_模型对比分析.md`

> **最终总结：** 我们不是简单“用神经网络替代公式”，而是把地表时序预测、垂直大气结构学习、GNSS 湿延迟观测和可审计部署评估连接成了一条完整研究链。真实廓线条件下，ProfileTransformer 展现了很强的结构学习能力；在更严格的历史回放部署条件下，第一阶段预测的 PS/TS/WPS 也已把第二阶段稳定推进到优于 GPT3 的水平。