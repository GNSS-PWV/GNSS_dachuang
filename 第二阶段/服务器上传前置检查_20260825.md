# 服务器上传前置检查（2026-08-25）

## 当前结论

暂不上传并启动正式重训。原因不是网络问题，而是正式训练输入尚未闭环：

1. 固定使用 2026-08-23 IGRA 目录版本审计后，365 个目标站点中 363 个 ZIP 通过 CRC；
   `ICM00004018`、`POM00008579` 在该版本公开目录中没有匹配文件。
2. 有效 ZIP 已统一放入 `D:\gnss水汽反演\igra_batch_20260824`，其中手动下载的 232 个有效文件已同步完成。
3. 363 个有效 IGRA ZIP 已转换为实验性 `*_met.txt` 垂直 profile；全量字段预检通过，共 1,310,588 个 profile。13 个站点只有表头、无可接受 profile。
4. 实验性严格切分已生成：`train=584904`、`test_2017=198654`、`val_2018=198262`、`val_leave_station=110230`、`val_2019=218538`。实际核心站点 350 个，留站点 35 个。
5. EC 数据仍未提供。ERA5 真实 NetCDF 下载、变量/层次核验和积分链路也尚未完成，因此不能宣称端到端部署输入已准备好。
6. 本机默认 Python 没有 PyTorch，但 `torch_gpu` 环境已验证 CUDA 和模型前向可用；正式训练仍建议在同济 GPU 分区通过 SLURM 执行。

## 已生成的审计产物

- `igra_batch_20260824\final_audit_20260823.csv`
- `igra_batch_20260824\final_audit_20260823.json`
- `igra_batch_20260824\final_audit_20260823.txt`
- `第二阶段\audit_igra_20260823_final.py`

审计政策：站点是否存在以 2026-08-23 目录为准；ZIP 必须通过 CRC；不因 NCEI 后续版本的文件大小变化否定有效副本。

## 上传前必须完成

1. 老师确认 IGRA 重建的 PWV/ZWD 是否允许作为实验训练标签，或提供正式 profile/标签。
2. 确认实际核心站点口径下的 `floor(350*0.1)=35` 留站点规则。
3. EC 到位后再进行外部数据对齐；ERA5 至少完成一个区域、一个日期、变量和层次的真实 NetCDF 积分验收。
4. 获取本次会话可用的同济服务器连接目标和登录方式后，再上传代码、manifest 和冻结后的 profile 数据。当前本机没有保存服务器地址或 SSH 别名。

本机可以使用 `第二阶段\run_strict_train_torch_gpu.ps1` 做实验性小规模入口验证；该包装仅用于 Windows 环境兼容，不改变正式训练的标签确认和服务器要求。

## 无凭据服务器操作模板

以下命令仅作服务器连接完成后的模板，不包含账号、密码、Token 或私钥：

```bash
ssh -p <PORT> <USER>@<HOST>
mkdir -p /share/home/<USER>/dachuang_pwv/phase2_strict_20260825
```

上传内容应先包括代码、切分 manifest 和校验报告；原始 ZIP 是否上传，取决于服务器磁盘预算和老师对实验标签口径的确认。正式训练必须通过 GPU 分区的 `sbatch` 提交，不能在登录节点直接运行。
