# 严格数据切分复核报告（2026-08-22）

## 复核对象

```text
第二阶段\result_strict_xlsx_split_20260819\split_manifest.json
第二阶段\result_strict_xlsx_split_20260819\file_assignments.csv
```

数据源目录：

```text
PWV传统模型情况\2014
PWV传统模型情况\2015
PWV传统模型情况\2016
PWV传统模型情况\2017
PWV传统模型情况\2018
PWV传统模型情况\2019
```

## 独立复核结果

- assignment 总数：1812；
- 原始 XLSX 文件数：1812；
- 缺失源文件：0；
- assignment 中不存在的文件：0；
- 重复 assignment：0；
- 留站点数：36；
- 剩余站点数：329；
- 留站点与剩余站点交集：0；
- 复核状态：**PASS**。

## 集合边界

| 集合 | 文件数 | 实际站点数 | 年份 |
|---|---:|---:|---|
| train | 800 | 314 | 2014–2016 |
| test_2017 | 282 | 282 | 2017 |
| val_2018 | 274 | 274 | 2018 |
| val_leave_station | 147 | 36 | 2014–2018 |
| val_2019 | 309 | 309 | 2019 |

## 重要说明

1. 这里是“站点年度 XLSX 文件级”切分。文件内的所有行会整体属于同一集合，不会把同一站点同一年份文件拆到不同集合。
2. `train` 实际出现 314 个站点而不是 329 个，是因为有一部分剩余站点在 2014–2016 没有对应原始年度文件；这属于数据覆盖不完整，不是切分错误。
3. `val_2019` 只有 309 个站点，是因为 2019 原始目录中实际有 309 个站点年度文件；脚本没有虚构缺失站点的数据。
4. 这些 XLSX 文件主要字段是 `doy, h, PWV`，目前适合进行站点/年份数据审计和 PWV 目标核对，不能直接替代第二阶段所需的完整探空垂直廓线（`ELV, TS, PS, WPS, ZWD...`）。

## UCAR 授权状态

截至 2026-08-22：

- 没有运行 `earthscope_auth_manual.py`；
- 没有读取 UCAR/EarthScope 账号、密码、Token、key 或认证文件；
- 没有下载需要 UCAR 授权的数据；
- UCAR 站表异常记录和重复站点的处理口径仍需老师确认。

这不是遗漏，而是按照 `D:\myai\AGENTS.md` 的凭据安全边界主动保持未授权状态。
