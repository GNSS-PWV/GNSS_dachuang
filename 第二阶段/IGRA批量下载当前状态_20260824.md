# IGRA 批量下载当前状态

更新时间：2026-08-24

## 当前覆盖

- 目标站点：363 个（依据 2014--2018 严格站点清单）。
- 按 NCEI 实时目录字节数一致：305 个。
- 完全缺失：55 个。
- 已有断点但长度不完整：3 个。
- 缺失与断点剩余传输量：约 7.87 GB。
- D 盘剩余空间：约 16 GB，继续下载必须保持小批次并监控空间。

## 完整性结论

字节数一致不等于 ZIP 内容完整。此前发现部分文件虽然长度一致，但 CRC 校验失败；已验证的修复流程是：

1. 删除已确认 CRC 损坏的 ZIP；
2. 从 NCEI 重新下载，不使用旧断点；
3. 使用 `zipfile.testzip()` 复核 CRC；
4. 通过后才允许解析和上传。

批量 CRC 扫描工具：`第二阶段/igra_batch_inventory.py`。

## 暂缓站点

以下站点在当前 VPN 链路上出现多次大文件超时或传输回退，保留断点但暂不阻塞其他工作：

- `CAM00071816`
- `ITM00016320`
- `USM00072214`

`CAM00071109` 已重新下载并通过 CRC，现不再列入暂缓清单。

## 继续策略

- 优先修复小于约 50 MB 的 CRC 损坏文件；
- 大文件单站点运行，避免一个站点阻塞整批；
- 下载完成后重新生成完整 CRC inventory；
- 在 CRC 全部通过前，不解压生成正式训练输入，不上传同济服务器；
- IGRA 重建标签仍属于实验性结果，不能替代老师正式标签或 ERA5 校准结果。

## 估时

剩余首轮下载约 7.87 GB；在当前链路下仅首轮落地预计仍需数小时。CRC 损坏文件的重下耗时取决于损坏数量，不能用字节覆盖率直接推算最终完成时间。
