# 给老师的资料请求完整模板（2026-08-19）

以下内容可以直接发送给老师，也可以根据老师习惯拆成微信消息或邮件。

---

## 版本一：完整正式版

老师您好，我这边已经完成了目前新增 GNSS 资料的本地接入和离线检查。您提供的 `config_v2.yaml`、IGS、EUREF、澳大利亚、SuomiNet 和 UCAR 站表都已经接入下载程序默认目录，并完成了文件一致性校验。

按照您的建议，下一步准备采用 2019 年 1 月、4 月、7 月和 10 月，优先选择美国、欧洲或澳大利亚区域，避免直接下载全球 ERA5；ERA5 后续按区域和气压层下载温度、气压、比湿以及 geoheight/位势高度；训练数据使用探空气球资料，GNSS 数据只用于独立验证。

为了继续完成二阶段模型训练和验证，麻烦老师协助提供或指出以下资料：

### 一、探空气球训练数据（最重要）

1. 探空气球训练数据文件，或具体所在目录/服务器路径；
2. 文件格式，例如 CSV、TXT、Excel、NetCDF 或其他格式；
3. 字段说明，最好包括：
   - 探空时间；
   - 探空站名称或站点编号；
   - 纬度、经度；
   - 气压；
   - 温度；
   - 比湿，或可替代计算比湿的露点温度/相对湿度；
   - 高度或位势高度；
   - 可用于计算整层可降水量 PWV 的变量；
4. 数据覆盖的起止年份和时间分辨率；
5. 探空站点的地理范围和站点坐标表；
6. 缺测值、异常值和单位说明；
7. 训练标签是直接提供 PWV，还是需要根据垂直廓线积分计算；
8. 是否有推荐的质量控制规则或已经处理好的版本。

### 二、训练/验证时间划分

还请确认以下研究口径：

1. 2019 年是否必须全部作为独立验证年份；
2. 探空气球训练数据是否可以使用 2019 年以前的数据；
3. 训练集、验证集和测试集建议按年份划分，还是按站点/区域划分；
4. 是否要求某些站点或月份完全不参与训练，以保证验证独立性。

### 三、ERA5 数据和积分输入说明

目前已经找到 ERA5 下载和 MATLAB 积分代码，但还没有真实 NetCDF 样本。麻烦老师确认：

1. ERA5 应使用哪个数据产品或下载方式；
2. 压力层范围和层数要求；
3. 温度、气压、比湿、geoheight/位势高度对应的变量名称；
4. 是否有已经下载好的 ERA5 样例 NetCDF 文件；
5. `ERA5_obtain` MATLAB 程序对应的输入文件命名、变量维度、时间格式和单位；
6. 是否已有推荐的区域经纬度范围，例如美国、欧洲、澳大利亚各自的边界框。

如果老师手头已经有一个可以正常运行的 ERA5 NetCDF 样例，优先提供一个单日或单时次样例即可，不必先提供整年数据。

### 四、GNSS/UCAR 站表口径确认

离线检查 UCAR 站表时发现：

- 1 条非数值表头样式记录；
- 1 条经纬度范围异常、疑似列错位记录；
- 9 个重复站点 ID。

我暂时没有修改老师提供的原始文件。麻烦老师确认：

1. 异常记录是否删除；
2. 重复站点是否代表不同版本、不同坐标或重复导出；
3. 如果需要去重，应保留哪条记录；
4. 是否有对应的原始站点清单可以用于核对。

### 五、已有成果或参考文件（如有）

如果已有以下资料，也请一并提供或指出路径：

- 探空气球到 PWV 标签的处理脚本；
- ERA5 分层下载脚本或成功下载示例；
- ERA5 垂直积分脚本及输入输出示例；
- 训练数据预处理脚本；
- 第二阶段模型训练配置和已有 checkpoint；
- 站点坐标、站点名称与数据源对应表；
- 数据质量控制和异常值剔除说明。

目前我会先在本机进行一个免认证 GNSS 单日探针，不会直接进行全球 ERA5 下载，也不会启动训练。等探空气球数据、ERA5 样例和训练划分口径明确后，再考虑连接同济超算中心进行训练。谢谢老师！

---

## 版本二：微信简洁版

老师您好，我这边已经把您提供的 GNSS 站表和 `config_v2.yaml` 接入程序，并完成了离线检查。下一步准备按您的建议，使用 2019 年 1、4、7、10 月，在美国、欧洲或澳大利亚做区域化验证，ERA5 按区域和气压层下载温度、气压、比湿、geoheight；探空气球用于训练，GNSS 仅用于验证。

目前继续二阶段训练最需要的是：

1. 探空气球训练数据文件或所在路径；
2. 文件格式和字段说明，尤其是时间、经纬度、气压、温度、比湿/湿度、高度或位势高度、PWV 标签；
3. 数据覆盖年份、站点坐标、缺测值和单位说明；
4. 训练集、验证集和测试集如何划分，2019 年是否需要全部留作独立验证；
5. 是否有已经成功运行的 ERA5 NetCDF 样例、变量维度说明和 MATLAB 积分输入示例；
6. UCAR 站表中异常记录和重复站点的处理口径。

如果暂时不能提供完整数据，先提供一个小样例和字段说明也可以。我会先完成本地单日 GNSS 探针，不会直接批量下载全球 ERA5。等数据链路确认后，再申请连接同济超算训练。谢谢老师！

---

## 版本三：只询问最关键文件

老师您好，目前 GNSS 站表和下载配置已经接入。为了继续二阶段模型训练，当前最需要您提供的是：

1. 探空气球训练数据文件；
2. 数据字段说明和单位；
3. 探空站坐标及时间范围；
4. PWV 标签是否已提供，还是需要根据垂直廓线计算；
5. 2019 年是否全部作为独立验证集；
6. 一个可正常运行的 ERA5 NetCDF 样例及其变量/维度说明。

另外，UCAR 站表中有一条异常坐标记录和 9 个重复站点 ID，麻烦确认是否有推荐的清洗和去重规则。谢谢老师！

---

## 建议老师优先提供的最小文件包

如果老师希望先给最少资料，优先级如下：

### 第一优先级：必须

```text
1. 探空气球训练数据样例或完整文件
2. 探空气球字段说明/单位说明
3. 探空站点坐标表
4. 训练、验证、测试年份划分要求
```

### 第二优先级：强烈建议

```text
5. 一个可读取的 ERA5 NetCDF 样例
6. ERA5 变量、维度和单位说明
7. ERA5 积分脚本的输入输出示例
```

### 第三优先级：用于提高验证质量

```text
8. UCAR 原始站表或异常记录说明
9. UCAR 重复站点去重规则
10. GNSS 与探空/ERA5 站点匹配表
11. 数据质量控制规则
```

## 安全提醒

请老师不要通过聊天直接发送账号、密码、Token、私钥或认证文件。认证信息应由用户在本机或服务器的受控环境中配置，只需要提供文件路径、变量说明和运行方法即可。
