# 当前可做任务进展（2026-08-22）

## 已完成且不依赖新文件的工作

1. 建立 PPT 素材包：`PPT素材_当前情况_20260822/`，包含逐页文字、核心数据表、8 张图表和口径说明。
2. 保留旧版 `第二阶段/train.py` 不变，新增严格协议训练入口：`第二阶段/strict_train.py`。
3. 新增训练前预检：`第二阶段/strict_profile_preflight.py`。
4. 严格协议固定为：留站点 10%（seed=42）+ 剩余站点按 2014–2016/2017/2018 划分 + 2019 独立验证。
5. Scaler 明确只使用 `train` 集拟合，四个评估集合只复用训练集 Scaler。
6. 训练入口分别输出 2017 测试、2018 常规验证、留站点验证和 2019 独立验证的指标与预测明细。
7. 已完成 Python 语法检查、合成数据严格切分测试、本地探针字段预检。

## 验证结果

- 新增脚本 `py_compile`：PASS；
- 合成 10 站、2014–2019 数据：五集合切分 PASS，留站点数为 1，年份边界正确；
- 本地 `第一阶段/xg_test` 探针：4077 条 profile、字段检查 PASS；但仅包含 2014 年，因此 2017、2018、2019 集合为空，只能作为代码探针，不能作为正式训练。
- 上下文压缩服务：`http://127.0.0.1:8787/healthz` 返回 `status=ok`。

## 当前限制

- 本地未安装 PyTorch（`torch_available=False`），不能在本机启动训练；正式训练应在同济超算执行。
- 本地探针缺少完整年份覆盖，不能据此产生新协议精度结论。
- 完整 profile 数据到位前，不执行正式重训、不下载大规模外部数据。

## 数据到位后的直接命令

```bash
python strict_profile_preflight.py \
  --data_dir /path/to/profile/data \
  --require_all_splits

python strict_train.py \
  --data_dir /path/to/profile/data \
  --output_dir result_strict_train_20260822 \
  --require_all_splits
```

预检必须确认五个集合均非空，并检查最终站点互斥、年份边界和 2019 独立性后，才提交超算训练。

## 子代理与压缩服务记录

本轮尝试启动只读代码审查子代理，但 CC-Switch 返回 `503 No available channel`，子代理未修改文件。主流程已完成等价审查和验证。上下文压缩服务正常，无需改动其配置。
