# 当前任务计划更新（2026-08-22）

## 当前状态

已完成老师指定的站点/年份切分规则实现，并对现有 2014–2019 XLSX 文件级数据完成独立复核。切分规则：

- 核心数据 2014–2018；
- 2014–2018 站点全集 365 个；
- seed=42；留站点 `floor(365*0.1)=36`；
- 留站点 2014–2018 全部进入留站点验证；
- 剩余站点 2014–2016 训练、2017 测试、2018 常规验证；
- 2019 全部独立作为最终验证。

## 已生成文件

```text
第二阶段\strict_dataset_split.py
第二阶段\strict_xlsx_dataset_split.py
第二阶段\result_strict_xlsx_split_20260819\split_manifest.json
第二阶段\result_strict_xlsx_split_20260819\file_assignments.csv
第二阶段\result_strict_xlsx_split_20260819\independent_audit_20260822.json
第二阶段\严格数据切分复核报告_20260822.md
```

独立审计状态为 `PASS`：1812/1812 个源文件均被且仅被分配一次，站点互斥、年份边界和 2019 独立性均通过。

## 当前不能直接进入正式训练的原因

- `第一阶段\xg_test` 只有 10 个站点且只有 2014 年；
- `第二阶段\radiosonde_2019` 只有 4 个 2019 年站点；
- `PWV传统模型情况\2014–2019` 的 XLSX 主要是 `doy,h,PWV`，缺少第二阶段 Transformer 训练所需的完整探空垂直廓线字段；
- 默认 Python 没有 `torch`；已确认 Anaconda `torch_gpu` 环境可用 CUDA，可本地做小规模入口验证；正式全量训练仍应在同济 GPU 分区执行；
- EC 数据尚未提供。

## UCAR 状态

UCAR 尚未授权。未运行手动授权代码，未读取任何认证材料，也未发起需要 UCAR 凭据的数据下载。后续若老师明确授权并完成凭据配置，再单独执行授权流程；在此之前继续做离线工作。

## 后续顺序

1. 获得 2014–2018 完整探空廓线或明确转换脚本；
2. 将 profile 级数据接入 `strict_dataset_split.py`，生成 profile assignment；
3. 验收 scaler 只在 train 拟合，验证/测试/2019 不参与拟合；
4. 接入 ERA5 小区域、少变量、单日探针；
5. EC 到位后完成对齐；
6. 数据闭环后再连接同济超算训练。
