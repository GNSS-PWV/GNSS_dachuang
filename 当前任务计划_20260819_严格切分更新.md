# 当前任务计划（严格数据切分更新，2026-08-19）

## 已完成

1. 重新核对并遵守 `D:\myai\AGENTS.md`：不读取账号、密码、Token、key 或认证文件；不运行 UCAR 手动授权；不启动真实 ERA5 下载和训练。
2. 只读审计项目数据目录，确认最完整的 2014–2019 站点年度数据入口为：
   `PWV传统模型情况\2014` 至 `PWV传统模型情况\2019`，共 1812 个 XLSX 站点年度文件。
3. 新增 `第二阶段\strict_dataset_split.py`：面向现有 `*_met.txt` 垂直廓线数据，执行老师指定的站点/年份严格切分。
4. 新增 `第二阶段\strict_xlsx_dataset_split.py`：面向现有年度 XLSX 文件，生成可审计的站点年度文件级切分清单。
5. 已生成并验证：
   - `第二阶段\result_strict_split_probe_20260819\`
   - `第二阶段\result_strict_xlsx_split_20260819\`
6. 已完成最小模拟数据验证：随机种子 42 可复现；留站点与剩余站点互斥；年份边界正确；2019 独立；无重复分配。
7. 上下文压缩服务健康检查通过：`http://127.0.0.1:8787/healthz` 返回 `status=ok`。

## 老师指定的最终划分规则

- 核心数据只保留 2014–2018。
- 核心站点总数为 365；留站点数为 `floor(365*0.1)=36`；随机种子 42。
- 36 个留出站点的 2014–2018 全部文件进入 `val_leave_station`，不参与训练、2017 测试或 2018 常规验证。
- 剩余 329 个站点：2014–2016 为 train，2017 为 test_2017，2018 为 val_2018。
- 2019 的 309 个站点年度文件全部进入独立 `val_2019`，不参与训练、测试或常规验证。

当前 XLSX 文件级结果：

| 集合 | 文件数 | 站点数 | 年份 |
|---|---:|---:|---|
| train | 800 | 314 | 2014–2016 |
| test_2017 | 282 | 282 | 2017 |
| val_2018 | 274 | 274 | 2018 |
| val_leave_station | 147 | 36 | 2014–2018 |
| val_2019 | 309 | 309 | 2019 |

留站点文件缺少部分年份属于原始数据覆盖不完整，不是切分错误；切分脚本不会虚构不存在的文件。

## 当前边界与阻塞

1. 现有 XLSX 主要字段为 `doy, h, PWV`，可用于站点/年份清单和目标数据审计，但不够直接作为第二阶段 `data.py` 所需的完整垂直廓线训练输入。
2. 第二阶段训练输入仍需要 2014–2018 的完整探空廓线文件（至少包括 `TIME,YEAR,DOY,LAT,LON,ELV,TS,PS,WPS,ZWD,ZHD,ZTD,PWV,Tm`），或明确的转换脚本/数据说明。
3. EC 数据仍缺失，后续由老师提供。
4. UCAR 仍未授权；未执行授权代码，也未读取认证材料。
5. 未连接同济超算；当前只完成本地低风险审计和切分工具验证。训练前需要完整输入、字段核对和用户明确连接服务器。
6. 当前本地环境没有 `torch`，所以没有在本地启动第二阶段训练；这是预期的环境限制，不影响清单生成。

## 下一步

1. 老师提供完整 2014–2018 探空廓线或转换所需字段后，用 `strict_dataset_split.py` 重新生成 profile 级清单。
2. 用独立脚本检查每个集合的样本数、站点数、年份范围、重复时间和站点交集。
3. 处理 UCAR 前，先确认老师同意异常行、重复站点 ID 和字段错位记录的处理口径；不自行清洗原始站表。
4. ERA5 只按小区域、单日、少变量探针下载；需要手动授权/凭据时先由用户完成授权。EC 到位后再接入。
5. 数据链路闭环后，再请求用户连接同济超算并启动训练。
