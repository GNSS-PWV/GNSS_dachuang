# VPN 与公开数据下载状态（2026-08-23）

## 订阅尝试

用户提供的订阅地址已尝试请求两次：PowerShell 请求和 curl 方式均未建立有效连接，返回“基础连接被关闭/连接被意外关闭”。订阅内容没有输出到对话，也没有读取或保存其节点内容。

本机检查未发现：

- Clash / Mihomo / Clash Verge；
- sing-box / V2Ray / Xray；
- 常用代理监听端口 7890、7891、7892、1080、10808。

因此当前只是订阅地址请求失败，尚未形成可供下载器使用的本地代理端口。

## 已完成的本地下载

在未使用 VPN 的情况下，NOAA/NCEI IGRA 单站文件已通过断点续传完成：

```text
AEM00041217-drvd.txt.zip
```

完整大小：50,692,564 bytes；ZIP 检查通过；解析和实验性 profile 重建已完成。

## 当前停止点

单站下载耗时约 8.5 分钟，网络速度不适合直接下载 363 个站点。没有启动批量下载，也没有上传服务器。

## 恢复条件

请在本机启动支持该订阅格式的代理客户端，并确认 HTTP 或 mixed 代理端口，例如 `127.0.0.1:7890`。端口可用后，继续使用 `igra_public_profile.py` 的断点续传功能，先下载第二个站点测速，再决定批量下载。

所有下载仍先落地到本地，完成文件大小/ZIP 校验/字段审计后，才考虑上传同济服务器。

## 2026-08-24 VPN 下载进展

Clash Verge Mihomo 已检测到本地代理端口：

```text
127.0.0.1:7897
```

通过该代理完成了 NOAA/NCEI IGRA `derived-por` 文件下载。当前审计：

- 目标公开核心站点：363；
- 完整文件：274；
- 已下载容量：约 11.07 GB；
- 未完成：89；
- 已完成文件均按 HTTP `Content-Length` 校验；
- 当前没有把部分下载文件当作有效数据使用。

小文件下载约 4–15 秒；部分 70 MB 左右文件耗时约 2–8 分钟。期间出现少量 curl 35/56 的临时 TLS 失败，断点重试已成功补齐已重试项。

当前批次已停止，原因是继续顺序下载大文件速度过慢。下载目录：

```text
D:\gnss水汽反演\igra_batch_20260824
```

审计文件：

```text
igra_batch_20260824\coverage_status.json
```

后续恢复命令仍为：

```powershell
powershell -ExecutionPolicy Bypass -File .\第二阶段\download_igra_batch.ps1 -Limit 20 -MaxTime 1800
```

脚本会自动跳过完整文件，并从断点继续未完成文件。
