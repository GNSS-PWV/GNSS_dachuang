# 数据下载器与 ERA5 链路：离线可靠性审查（2026-08-17）

> 范围：只对现有脚本进行静态阅读和已有产物复核；未联网、未运行下载器、未打开认证或密钥文件、未修改任何生产下载逻辑。

## 1. 当前推荐入口与边界

- **推荐正式入口：** `数据计算代码/download_gnss_stations.py`。
- **不应作为正式入口：** `数据计算代码/gnss_downloader.py` 自带的示例式 `main()`。其尾部直接引用未在该函数创建的 `downloader_ngl`，直接执行可能触发未定义变量错误。
- `GNSSDownloader` 初始化会直接读取传入的 `config_path`；因此原始 `download_lables/config_v2.yaml` 仍是正式运行的必备文件。临时最小 YAML 只能用于隔离探针，不能伪装成正式配置。
- IGS 分支 `fetch_igs_whu()` 直接从本地站点清单加载 `igs_whu`；当站表为空时会返回无数据。因此 `GNSS_list/IGSwhu_formatted.txt` 是 IGS 正式下载的硬依赖。

## 2. 已识别的可靠性风险

### 2.1 “空结果仍记为完成”的续传语义风险

`download_gnss_stations.py` 的来源循环中：

1. 当一个来源返回空 DataFrame 或 `None` 时，只打印“无数据”；
2. 随后仍将该来源追加到 `_done.json`；
3. 若当天所有来源均为空，程序继续完成后续流程并最终返回退出码 0。

这会把“真实无数据”“站表缺失”“来源临时异常后被吞掉”“解析失败导致空结果”等状态混到相同的完成语义中。后续自动化不能仅凭退出码或 `_done.json` 判断下载成功，必须同时检查：

```text
输出站点文件数 > 0
输出记录数 > 0
_done.json 中的来源/日期
每来源的行数、异常数和空结果数
期望日期的时间覆盖
```

本轮不直接改变这个语义，因为“空结果是否可重试、何时视为失败、是否写 manifest”会影响断点续传和批量运行，属于应先做最小复现实验并审阅的生产行为变更。

### 2.2 IGS 站点清单缺失会静默退化为无数据路径

正式入口中 `fetch_igs_whu()` 在本地站表为空时返回 `None`。主循环随后可将其记录为“完成”。因此缺失 `IGSwhu_formatted.txt` 时，可能出现进程正常结束但不产出 IGS 数据的情况。

恢复 IGS 前必须：

```text
补齐 GNSS_list/IGSwhu_formatted.txt
→ 执行 strict preflight
→ 单日、单源、隔离输出目录测试
→ 核对输出记录、站点数与 _done.json
```

### 2.3 旧版下载器 CLI 不可作为批处理入口

`gnss_downloader.py` 的末尾 `main()` 是示例流程，依赖未在该函数定义的对象，并混合多数据源示例、ERA5 处理和认证源示例。它不适合自动化批处理、回归测试或论文复现实验。

当前建议只在将来经最小重构、明确 CLI 参数、退出码和 manifest 语义后，才考虑把它变成可支持的入口；在此之前应显式使用 `download_gnss_stations.py`。

### 2.4 ERA5 脚本的环境和范围尚未冻结

- `download_era5_2018_2024.py` 依赖 `boto3`，并写死 POSIX 风格输出路径 `Path("/f/era5_single_level")`；Windows 环境需要先明确项目内可用本地目录。
- `download_era5_hk_cds.py` 依赖 `cdsapi`，当前固定输出目录为 `F:/era5_hk_cds`；当前机器上 F: 不可用，预检应视为 BLOCKER。
- CDS 脚本中的区域、年限、变量和输出文件大小都属于实验协议字段，不能因本地可用空间、下载速度或结果表现而临时改变后再与正式结果混用。

## 3. 不依赖新数据即可执行的最小离线测试清单

在不联网、不读取认证材料的条件下，资料到位后可先执行：

1. `preflight_data_pipeline.py --strict`：确认配置、站表、依赖、路径和积分资源；
2. `python -m py_compile`：检查 `download_gnss_stations.py`、`gnss_downloader.py`、ERA5 脚本和积分相关 Python 文件；
3. 对 GNSS 入口进行参数验证测试：空来源、非法日期、结束日期早于开始日期应返回非零退出码；
4. 创建隔离的模拟输出目录，验证 `_done.json` 不会被旧运行记录误复用；
5. 仅在不用认证源的前提下，设置一个单日、单源、小范围的干跑验收标准：非零站点文件、非零记录数、时间字段可解析、manifest 对应；
6. 对 ERA5 脚本先验证目录、范围配置、依赖版本和预计文件数量/大小，之后才允许网络请求。

第 5、6 项中的实际网络调用应等资料、协议和路径补齐后再执行；若持续低速、超时、TLS 或域名不可达，应停止并请求开启 VPN，而不是直接转多年批量任务。

## 4. 当前建议的行动边界

### 可以继续

- 使用本审查文档完善论文/交接中的数据链路限制；
- 执行无网络的语法、参数和 preflight 测试；
- 冻结未来验证的数据字典、ERA5 范围和输出清单；
- 在资料补齐后做单日、单源、隔离目录验收。

### 暂不执行

- IGS 批量下载；
- 多年 ERA5 下载；
- 认证源或认证辅助脚本；
- 对 `_done.json`、来源失败语义或 PWV 质量规则的生产修改；
- 用临时探针数据支撑正式模型精度结论。
