# 项目进展检查点：弱站输入诊断与数据管线预检（2026-08-17）

> 用途：作为后续连续研究的压缩上下文。仅记录可复核研究状态；不包含认证材料、账号、密钥或下载凭据。

## 1. 正式研究口径（保持不变）

- 严格部署结果范围：2014–2019 年；
- 共同样本：110,928；
- 六年并集站点：36；
- 当前可部署主方案：`clim_surf_p1`；
- `clim_surf_p1` RMSE：0.242729 mm；GPT3 RMSE：0.346177 mm；相对降低 29.88%；
- 33/36 个站点优于 GPT3；
- `real`、`real_surf_p1` 属于 oracle 口径，不可作为部署精度；二阶段仍使用真实 GNSS ZWD；Tm 不是二阶段输入。

## 2. 本轮完成的可复核工作

### 2.1 一阶段地表输入误差关联诊断

新增并验证：

```text
第二阶段/diagnose_strict_station_surface_inputs.py
```

输出目录：

```text
第二阶段/result_p1deploy_ft_strict_20260817/analysis_2014_2019/station_robustness_20260817/
```

核心输出：

```text
station_surface_input_diagnostics.csv
弱站一阶段地表输入误差关联诊断_20260817.md
```

脚本保证：

1. 对 `pwv_true`、`pwv_clim_surf_p1`、`pwv_gpt3` 统一数值化，并只保留三者均为有限数值的单一共同样本；
2. 同时输出 `pwv_common_N`、`N_clim_surf_p1`、`N_gpt3` 并在站点内不相等时中止；
3. PS、TS、WPS、Tm 在该共同 PWV 队列中，按各自真值/P1 同时有效的子集计算 `N`、RMSE、MAE、Bias；
4. Bias 定义为 `P1 − true`：正值表示 P1 偏高；
5. Tm 明确为“仅诊断，不是二阶段输入”；
6. 报告明确为事后探索性关联分析，禁止将结果表述为因果证明或用于当前测试集的逐站选模。

回归验证结果：

```text
common_samples = 110928
stations = 36
weak_stations = 3
clim_surf_p1 与既有站点汇总 RMSE 最大差 = 0
GPT3 与既有站点汇总 RMSE 最大差 = 0
```

当前未优于 GPT3 的三个站点：

```text
COM00080222
SFM00068842
SAM00040417
```

### 2.2 全体站点相对位置的探索性上下文

新增并验证：

```text
第二阶段/summarize_weak_station_input_context.py
```

输出：

```text
station_surface_input_error_rank_context.csv
弱站输入误差全体站点相对位置_20260817.md
```

排名规则：每项输入 RMSE 在 36 个站点中降序排列，`1/36` 表示误差最大。

三个弱站的相对位置：

| 站点 | PS RMSE 排名 | TS RMSE 排名 | WPS RMSE 排名 | Tm RMSE 排名（仅诊断） |
|---|---:|---:|---:|---:|
| COM00080222 | 2/36 | 25/36 | 32/36 | 29/36 |
| SFM00068842 | 7/36 | 20/36 | 18/36 | 13/36 |
| SAM00040417 | 26/36 | 5/36 | 2/36 | 12/36 |

解释边界：这些是值得在独立未来时段实验中重点复核的误差模式，**不是**“某个输入误差已经被证明导致弱站”的因果结论。

## 3. 本地小模型与协作规则执行状态

- 已重新阅读并遵循 `D:\myai\AGENTS.md`；
- 本轮脚本任务按中等复杂度、低风险处理，主代理完成最小范围编辑和验证；只读审阅任务交给独立子代理，且已在完成后关闭；
- 本地 `myai-coder-7b:q4` 曾因主机侧内存分配失败而无法启动；用户清理内存后，于 2026-08-17 成功完成两次本地低风险口径复核；
- 没有强迫失败的本地路径继续运行；没有启动并发 GPU 模型；
- 本文档是本轮的持久化压缩检查点。

## 4. 数据下载与 ERA5 积分链路状态

尚未启动 GNSS 或 ERA5 下载，因此没有速度结果，也没有 VPN 需求结论。

离线预检当前仍为：`BLOCKER = 6`、`WARN = 5`。

主要阻塞：

```text
缺少 src/data/download/gnss_downloader.py
缺少 download_lables/config_v2.yaml
缺少 download_lables/GNSS_list/
缺少 boto3
缺少 cdsapi
ERA5 CDS 脚本使用当前不可用的 F: 输出目录
```

另外：ERA5 现有脚本的目标范围（2018–2024 大范围或 2020–2024 香港区域）与正式 2014–2019 严格实验及未来时段外推设计不直接匹配，不能原样批量运行。

安全执行顺序：

1. 定位并补齐版本匹配的 `gnss_downloader.py`、配置、站点清单和静态资源；
2. 在隔离环境安装并记录 `boto3`、`cdsapi`、`pygeodesy` 版本，统一 Windows 输出路径；
3. 明确时间、区域、层次、变量和训练截止年后的验证范围；
4. 先做单日、单源、小范围、免认证测试；
5. 仅在出现持续低速、连接超时、域名不可达、TLS 或认证失败时，停止测试并提示开启 VPN；
6. 通过单样本 NetCDF 与 MATLAB 积分验证后，才允许批量下载/积分。

## 5. 下一步

1. 若缺失 GNSS 工具包及配置文件已补入工作区，重新运行：

```powershell
python .\数据计算代码\preflight_data_pipeline.py --strict
```

2. 若关键阻塞清零，再进行单日小样本下载连通性测试；
3. 在新 ERA5/NWP 剖面数据就绪后，实施“训练截止年 → 后续年份”的严格未来时段验证，并固定当前探索性弱站名单与判定规则，同时报告全体目标站点结果。

## 6. 新增：年度站点覆盖口径（严格共同样本）

新增并验证：

```text
第二阶段/analyze_strict_station_coverage.py
```

输出：

```text
annual_station_coverage_strict.csv
station_coverage_summary_strict.csv
station_year_coverage_matrix_strict.csv
年度站点覆盖口径说明_20260817.md
```

年度共同样本覆盖如下：

| 年份 | 有可比样本的站点数 | 共同 PWV 样本数 |
|---:|---:|---:|
| 2014 | 27 | 16,160 |
| 2015 | 28 | 17,508 |
| 2016 | 29 | 18,518 |
| 2017 | 30 | 19,693 |
| 2018 | 30 | 19,737 |
| 2019 | 30 | 19,312 |

因此，“36 站”严格指 2014–2019 年六年并集，不可表述为每年都稳定覆盖 36 个站点。该表描述既有严格部署文件中的共同可比样本可用性，不等同于原始 GNSS 观测完整率。

## 7. GNSS 下载器文件重新发现与导入修复（2026-08-17）

用户提示后重新盘点 `数据计算代码` 目录，已确认其中存在 `gnss_downloader.py`；项目根目录亦有字节级相同的副本。对两个副本进行了只读结构检查和编译检查：其包含 `GNSSDownloader`、ERA5 气象提供器及多源 GNSS 下载/解析逻辑，未检测到硬编码敏感赋值。未读取认证文件内容。

已对 `数据计算代码/download_gnss_stations.py` 进行最小兼容修复：优先保持原有 `src.data.download.gnss_downloader` 导入；仅当 `src` 包路径缺失时，回退导入同目录 `gnss_downloader.py`。其他模块缺失继续抛出。`--help` 已成功运行，证明导入链路已解除。

同步更新 `数据计算代码/preflight_data_pipeline.py`，使同目录独立下载器被识别为可用。预检由 `BLOCKER = 6` 降至：

```text
BLOCKER = 5
WARN = 5
```

尚未解除的关键下载阻塞：

```text
缺少 download_lables/config_v2.yaml
缺少 download_lables/GNSS_list/
缺少 boto3
缺少 cdsapi
ERA5 CDS 脚本固定 F: 输出目录不可用
```

在当前目录的只读文件名盘点中未找到上述 GNSS 配置、站点列表或 EGM96 资源；对整个 D: 的补充文件名搜索在 120 秒内未完成，未读取文件内容，也未启动任何下载。因此当前不能安全启动 GNSS 下载。

## 8. GNSS 配置/站点清单最小路径核查与 UCAR 连通性试验（2026-08-17）

本节是在不读取认证文件、不运行认证脚本的前提下，对 `gnss_downloader.py` 和 `download_gnss_stations.py` 做的 AST/离线运行核查。

### 8.1 配置与站点清单结论

1. `GNSSDownloader.__init__` 会直接打开 `config_path`，因此文件路径必须存在；但当前代码中 `self.config` 除加载赋值外没有其他引用。隔离目录中使用内容为 `{}` 的最小 YAML 已通过无认证初始化验证。因此，`config_v2.yaml` 的**文件存在性**是当前入口硬要求，其内容在现版本下载链路中不是已发现的运行时依赖。
2. `download_lables/GNSS_list/` 对 `igs_whu` 主入口是硬依赖：`fetch_igs_whu` 先调用 `load_station_list_from_file('igs_whu')`；若列表缺失会直接返回 `None`。隔离测试确认此条件下 IGS 下载函数调用次数为 `0`，即不会误触发网络下载。
3. 下载器虽具有 30 站内建 fallback，但它只在显式调用 `download_station_list()` 且远程列表均不可用后才返回；IGS 主入口不调用该方法。因此不能将该 fallback 当作正式 IGS 站点清单的替代品，也不能用于正式覆盖率或严格验证。
4. 当前命令行入口没有 `--dry-run` 参数；不能把 `--help` 或空启动误当作下载前完整校验。

### 8.2 隔离、单日、单源、免认证网络试验

为只验证公开源连通性，已执行一次**非正式数据探针**，不涉及 ERA5、不使用认证源、也不用于模型评估：

```text
来源：ucar_cosmic（公开、非认证）
日期：2020-01-01（即命令参数 20200101）
范围：单日、单源
PWV：使用 --no_pwv，跳过 ERA5 计算
配置：隔离目录内最小空 YAML
输出：数据计算代码/_tmp_ucar_network_probe_20260817/output/
```

结果：命令退出码 `0`，总耗时约 `31.8 s`；下载器请求的 4 个时次中成功获得 1 个文件，解析出 `18,766` 条记录、`393` 个站点文件，时间字段无无效值。没有发生持续低速、超时、TLS 或认证失败，因而本次**不需要 VPN**。

数据完整性边界：该源文件本身已经覆盖当天 `00:15` 至 `23:45`，但下载器报告仅 `1/4` 个请求成功；因此这只能证明公开源可达，不能宣称“四时次文件已完整获取”。另外，原始输出中有 `2,871 / 18,766` 条有限 PWV 为非正值（范围约 `-9.9` 至 `52.9 mm`）。这批探针数据保留原始值，尚未纳入研究结果；任何正式使用前必须冻结并验证质量控制规则。项目现有 ERA5 计算分支采用 `0 < PWV < 100 mm`，可作为后续是否统一 UCAR 质控的待决依据。

### 8.3 当前行动边界

- 可以继续对公开 UCAR 数据做离线结构、质量和覆盖率研究；
- 不能启动正式 IGS 批量下载，除非用户提供版本匹配的 `GNSS_list`；
- 不应为了绕过入口要求而把临时空 YAML 伪装成正式 `config_v2.yaml`；
- UCAR 非正 PWV 的处理涉及正式数据质量规则。规则路由将该修改判为 `difficulty=2, risk=2, route=CLOUD`；在没有云端决策结果前，不修改生产下载脚本，只保留该风险记录。

### 8.4 独立 Validator 复核结论

独立只读 Validator 已按 `D:\myai\.agents\prompts\validator.md` 完成静态复核（未联网、未下载、未触及认证逻辑），与上述核查一致，并补充两项工程风险：

1. `gnss_downloader.py` 自带的旧 CLI 主入口存在未定义变量路径，不能作为可用的正式入口；当前实际可验证的入口是 `download_gnss_stations.py`。
2. `download_gnss_stations.py` 对单个来源的初始化异常会在来源循环内捕获；缺失配置时可能出现“未产生数据但整体进程结束”的假成功风险。因此正式批量运行必须先执行预检，并检查输出行数、`_done.json` 与期望来源/日期的对应关系，不能仅凭进程退出码判定成功。

这两项属于下载器可靠性边界；未在本轮直接修改生产脚本。后续若要修改来源失败语义、补充 `--dry-run` 或统一 UCAR 质量控制，应先形成最小复现实验和决策包，再按高风险路由审阅。

### 8.5 UCAR 探针质量报告（离线复核）

已生成以下仅用于探针审计的文件：

```text
数据计算代码/_tmp_ucar_network_probe_20260817/ucar_probe_quality_report_20260817.md
数据计算代码/_tmp_ucar_network_probe_20260817/ucar_probe_station_quality.csv
数据计算代码/_tmp_ucar_network_probe_20260817/ucar_probe_quality_summary.json
```

对输出时间字段按其实际格式 `YYYYMMDDHHMM` 显式解析后，时间范围为 `2020-01-01 00:15` 至 `23:45`，393 个站点文件均无无效时间和重复时间；384 个站点有完整 48 条半小时记录，9 个站点为部分覆盖，最少 13 条。原始 PWV 中 2,872 条为非正值，15,894 条落入现有 `0 < PWV < 100 mm` 物理范围。报告只做标记，不删除或改写原始文件。

## 9. 当前缺失资源记录与恢复条件（2026-08-17）

### 9.1 已确认缺失

最近一次安全预检为：

```text
BLOCKER = 5
WARN = 5
```

当前阻塞项：

```text
数据计算代码/download_lables/config_v2.yaml
数据计算代码/download_lables/GNSS_list/
Python 依赖 boto3
Python 依赖 cdsapi
ERA5 CDS 脚本固定使用 F: 输出目录，但当前 F: 不可用
```

可选但建议补齐：

```text
数据计算代码/download_lables/data/geoids/egm96-5.pgm
Python 依赖 pygeodesy
```

### 9.2 资源语义

- `config_v2.yaml`：当前下载器要求文件存在且可解析；对现有两份下载代码，尚未发现其内容被后续读取。正式实验仍应优先使用老师提供的原始版本，不能把临时空 YAML 当作正式配置。
- `GNSS_list`：IGS 主入口的硬依赖。至少需要 `IGSwhu_formatted.txt`；缺失时 IGS 不会进入实际下载。
- EGM96：高程修正资源，当前属于警告，不是 UCAR 公开探针的启动硬依赖。
- `boto3`、`cdsapi`：分别对应现有 ERA5 公共下载脚本和 CDS 脚本；安装前还要先冻结 ERA5 范围与输出路径。
- 不需要提供认证内容。任何账号、密码、密钥、Token 或认证文件内容都不得发送到聊天或写入报告。

### 9.3 已采取的暂停边界

在资源补齐、范围冻结前：

1. 不启动 IGS 批量下载；
2. 不启动多年 ERA5 下载；
3. 不运行认证辅助脚本；
4. 不修改生产数据质量规则；
5. 不把 `_tmp_ucar_network_probe_20260817` 探针结果纳入 2014–2019 正式模型结果。

### 9.4 资源到位后的恢复顺序

```text
收到目录路径/非敏感文件
→ 重新运行 preflight_data_pipeline.py --strict
→ 核对文件版本、站点清单格式和 Windows 路径
→ 冻结 ERA5/未来验证协议
→ 安装并记录依赖版本
→ 单日、单源、小范围、隔离目录试验
→ 单个 NetCDF 与 MATLAB 积分验证
→ 通过后才允许批量下载和独立未来验证
```

### 9.5 当前可继续的无数据工作

在等待资源期间，优先继续：

- 严格部署结果的指标、年度、站点和弱站复核；
- 一阶段 PS/TS/WPS 输入误差与二阶段误差的探索性关联分析；
- 未来独立验证的预注册式规则和数据字典；
- GNSS/ERA5 下载脚本的离线接口、路径和失败语义审查；
- 论文/PPT 中模型架构、部署边界和结果口径的统一。

对应的给老师请求文字已保存为：

```text
给老师的项目资料请求模板_20260817.md
```

## 10. 严格结果一致性复核完成（2026-08-17）

已完成只读一致性审计并生成：

```text
第二阶段/result_p1deploy_ft_strict_20260817/analysis_2014_2019/strict_result_consistency_audit_20260817.json
第二阶段/result_p1deploy_ft_strict_20260817/analysis_2014_2019/严格结果一致性复核_20260817.md
```

审计摘要：

```text
common_samples = 110928
union_stations = 36
years = 2014–2019
metrics_rows = 6
max_annual_station_count = 30
weak_stations = 3
clim_surf_p1 RMSE = 0.242729 mm
GPT3 RMSE = 0.346177 mm
relative improvement = 29.88%
```

独立 Validator 只读复核结论：未发现结果文件的实质性不一致；36 个站点的站点键、样本数、比较标签和数值关系通过。需要保留的审计注意事项：`rmse_reduction_vs_gpt3_mm` 的定义必须是 `GPT3 - surface`；跨 CSV 的排名复核需要使用同一文件数值或显式浮点容差，不得直接使用无容差浮点相等。

该项工作不依赖新 GNSS/ERA5 数据，也没有修改生产模型或下载器。

## 11. 当前问题与后续恢复条件再次确认

当前数据链路仍缺少或不具备：

```text
数据计算代码/download_lables/config_v2.yaml
数据计算代码/download_lables/GNSS_list/IGSwhu_formatted.txt（及可能的完整 GNSS_list 目录）
数据计算代码/download_lables/data/geoids/egm96-5.pgm（建议补齐）
Python 依赖 boto3、cdsapi（按实际 ERA5 入口安装）
ERA5 CDS 脚本所写 F: 输出盘/路径
```

在老师提供路径或非敏感文件、且 ERA5 范围与输出目录冻结前，继续保持以下暂停边界：不启动 IGS 批量下载、不启动多年 ERA5 下载、不运行认证脚本、不修改生产质量规则、不把 UCAR 探针纳入正式结果。

等待期间继续推进：

- 论文/PPT 结果口径统一；
- 二阶段部署边界与架构说明；
- 弱站输入误差的探索性诊断；
- 未来独立验证预注册规则；
- 下载器离线接口和失败语义审查。

## 12. 无新数据工作推进：自动化审计、弱站稳健性与汇报口径（2026-08-17）

### 12.1 可复现严格结果一致性审计

新增脚本：

```text
第二阶段/audit_strict_result_consistency.py
```

该脚本只读取既有严格结果 CSV，不联网、不下载、不修改任何原始预测、模型或训练数据。它将此前人工审计中容易出错的两点固化为可执行断言：

1. `rmse_reduction_vs_gpt3_mm = GPT3 RMSE - clim_surf_p1 RMSE`；
2. 跨 CSV 数值比较使用明确浮点容差，输入误差名次只用排名文件自身的数值重算。

已通过 `py_compile` 和目标运行，生成：

```text
第二阶段/result_p1deploy_ft_strict_20260817/analysis_2014_2019/strict_result_consistency_audit_reproducible_20260817.json
```

验证结果：`PASS`；6 个模型、110,928 个共同样本、36 个站、6 个年度、3 个 `LOSS` 弱站均与既有权威结论一致。

### 12.2 固定弱站的逐年留一稳健性复核

新增脚本与结果：

```text
第二阶段/analyze_weak_station_leave_one_year_out.py
第二阶段/result_p1deploy_ft_strict_20260817/analysis_2014_2019/station_robustness_20260817/weak_station_leave_one_year_out_20260817.csv
第二阶段/result_p1deploy_ft_strict_20260817/analysis_2014_2019/station_robustness_20260817/weak_station_leave_one_year_out_summary_20260817.csv
第二阶段/result_p1deploy_ft_strict_20260817/analysis_2014_2019/station_robustness_20260817/弱站逐年留一稳健性复核_20260817.md
```

该复核只使用既有 `station_year_vs_gpt3.csv`，每次剔除一个已有可比年度后用 `N × RMSE²` 合并重算 pooled RMSE。结果：

```text
COM00080222：4 / 4 次留一后仍为 LOSS，差值范围 -0.145959 至 -0.136088 mm
SFM00068842：6 / 6 次留一后仍为 LOSS，差值范围 -0.056277 至 -0.050007 mm
SAM00040417：4 / 4 次留一后仍为 LOSS，差值范围 -0.012403 至 -0.005503 mm
```

因此，这三个站在当前六年评估期中的弱站汇总结论均不由单一年度驱动；但该结果仍是事后、同一历史评估期内的稳健性描述，不能替代未来独立验证，也不能构成输入误差或站点因素的因果归因。

### 12.3 论文 / PPT 统一引用口径

新增：

```text
论文PPT_当前权威结果口径卡_20260817.md
```

该文件固定了可直接使用的一句话主结论、唯一权威数值、方案定义、不可混用的 legacy 内容和必须披露的部署边界。论文/PPT 后续应以该文件和 `2014_2019严格回放式堆叠部署评估_成果汇总_20260817.md` 为准。

### 12.4 本轮本地模型与上下文纪律

本轮路由：

```text
自动化审计与逐年留一统计：coding / difficulty 2 / risk 1 / LOCAL 可执行
研究性弱站解释：research / difficulty 3 / risk 2 / 不强行让本地模型做最终研究结论
```

已串行调用本地 `myai-reasoner-7b:q4` 仅协助低风险的“无新数据工作优先级”草案；主结果均由可复现 Python 计算和独立 Validator 复核决定。调用后已检查 `ollama ps`，模型未驻留，符合 RTX 4060 单模型和及时卸载要求。未读取、输出或使用认证材料。

## 13. 下载器与 ERA5 链路离线可靠性审查（2026-08-17）

新增文档：

```text
数据下载器与ERA5链路_离线可靠性审查_20260817.md
```

该文档基于静态代码阅读，未联网、未运行下载器、未读取认证或密钥文件、未改动生产下载逻辑。确认的关键工程边界：

1. 正式 GNSS 入口应为 `download_gnss_stations.py`；旧 `gnss_downloader.py` 自带 `main()` 是示例式流程，直接依赖未定义对象，不能作为正式批处理入口。
2. `GNSSDownloader` 初始化直接读取 `config_path`；正式 IGS 运行仍需要原始 `config_v2.yaml`。
3. IGS 入口在 `IGSwhu_formatted.txt` 缺失时返回无数据；因此完整站表仍是硬依赖。
4. 当前来源循环把空结果也加入 `_done.json`，且当天全空时程序仍可能正常结束；后续自动化验收不能只看退出码或 manifest，必须检查站点文件数、记录数、来源/日期覆盖和时间字段。
5. ERA5 公共 S3 脚本仍有 POSIX 风格输出路径；CDS 脚本固定使用当前不可用的 F:。在范围、路径、依赖和单日验收冻结前，不启动网络下载。

本轮仅形成审计和离线验收清单，不直接改变生产失败语义、续传语义或质量规则；这些变更需要最小复现和审阅后再决定。

## 14. 无新增数据阶段：论文/PPT 图表包与结果溯源（2026-08-17）

### 14.1 任务路由与安全边界

本节工作分类为 `utility / coding`，难度 2、风险 1，满足 `LOCAL` 条件。目标是把已经冻结的严格历史回放结果转成可复现的论文/PPT图表与文件溯源清单；不改动训练、模型预测、原始 GNSS/ERA5 数据、下载器或质量控制规则。

本轮明确未读取、输出或运行 `.env`、账号、密码、Token、认证文件或下载密钥；未启动 GNSS/ERA5 下载。为遵循 RTX 4060 8GB 生命周期，曾串行调用本地 `myai-reasoner-7b:q4` 仅生成通用图表质检项（不用于研究结论），随后通过 `ollama stop` 和 `ollama ps` 确认无模型驻留、GPU 显存回到 0 MiB。

### 14.2 可复现图表流水线

新增脚本：

```text
第二阶段/generate_paper_ppt_figures.py
```

该脚本只读取下列冻结 CSV，并带有年度、模型数、110,928 共同样本、36 站并集、3 个固定 LOSS 站等输入断言：

```text
metrics_official36_2014_2019.csv
annual_metrics_official36_2014_2019.csv
station_robustness_20260817/station_vs_gpt3_summary.csv
station_robustness_20260817/annual_station_coverage_strict.csv
```

已通过 `python -m py_compile` 与目标运行，新增独立输出目录：

```text
第二阶段/result_p1deploy_ft_strict_20260817/analysis_2014_2019/paper_ppt_figures_20260817/
```

其中包含四组图的 PNG、PDF、SVG 三种格式，以及 `论文PPT图表与图注_20260817.md`：

1. 全局 PWV RMSE 对比：以斜线和图内声明显著区分 `real` / `real_surf_p1` oracle 与可部署候选；
2. 2014–2019 年度 RMSE：仅比较 `clim_surf_p1`、`clim_adj_p1`、GPT3 三个非-oracle 方案；
3. 站点级 `GPT3 - clim_surf_p1` RMSE 差值：正值明确表示 `clim_surf_p1` 更优，并将 33 WIN、3 LOSS 作为当前评估期探索性复核结果；
4. 年度可比样本数与有样本站点数：标题和图注明确 36 站是跨年度联合覆盖、年度实际为 27–30 站。

图注同时固定以下边界：历史回放式堆叠部署评估不等同于完全自治实时系统，也不是训练截止年后的未来独立验证；弱站现象不作因果归因或未来泛化承诺。

### 14.3 SHA256 结果复现与溯源清单

新增脚本：

```text
第二阶段/build_result_provenance_manifest.py
```

它使用显式 allowlist 而不是全盘扫描，计算 28 个用于当前图表和结论复核的脚本、结果 CSV、说明文档和图表文件的 SHA256、字节数和修改时间。产物为：

```text
paper_ppt_figures_20260817/result_provenance_manifest_20260817.json
paper_ppt_figures_20260817/结果复现与溯源清单_20260817.md
```

该清单明确说明：可复现已有 2014–2019 图表和冻结结果，不需要下载新数据；但不能证明上游训练数据、原始外部观测、未来数据或外部下载源的完整性。清单不纳入认证、密钥、Token、密码或 `.env` 路径。

### 14.4 独立复核结果

已完成三层验证：

1. 图表脚本的 `py_compile`、输入断言、输出存在性和 PNG 像素维度检查：`PASS`；
2. PowerShell 独立 SHA256 与图注口径复核：28 个 manifest 条目均存在且哈希一致、4 张 PNG 均非空、无敏感路径、110,928 样本 / 36 站并集 / 27–30 年度覆盖 / 3 LOSS 站与图表报告一致，结果写入：

```text
paper_ppt_figures_20260817/paper_ppt_bundle_independent_validation_20260817.json
```

3. 对图表所用源结果重新运行既有严格结果一致性审计，输出：

```text
paper_ppt_figures_20260817/strict_result_consistency_recheck_for_figures_20260817.json
```

结果：`PASS`，6 个模型、110,928 个共同样本、36 个站跨年度并集、2014–2019 六年度、年度最大 30 个有样本站点、3 个弱站、`clim_surf_p1` RMSE 0.242729 mm、GPT3 RMSE 0.346177 mm、相对改善 29.8829789% 均与权威口径一致。

后续论文/PPT 应优先引用 `论文PPT_当前权威结果口径卡_20260817.md` 与本图表包，而不能混入早期单年 legacy 结果。

### 14.5 独立 Validator 复核与最小加固

按 AGENTS.md 的非重叠只读复核要求，已安排独立 Validator 对图表脚本、溯源脚本、图表目录与权威口径卡进行审查。Validator 结论为 `PASS`：未发现 oracle 被误作部署精度、36 站跨年度并集被误作年度覆盖、legacy 单年数值混入、因果/未来泛化越界或 manifest 纳入敏感路径的问题；28 个列入 manifest 的条目与当前 SHA256 一致。

Validator 提出的唯一可选加固已采纳：`generate_paper_ppt_figures.py` 现在显式断言站点状态必须恰为 `WIN=33`、`LOSS=3`，不允许 `TIE`、缺失值或其他标签，以避免未来输入异常时图例仍显示固定数目。该改动不改变任何研究数值；随后已重新执行图表生成、manifest 生成和独立 SHA256/口径复核，最终均为 `PASS`。

## 15. 老师补充站表与 2019 四季区域化验证准备（2026-08-19）

### 15.1 老师新增研究建议

已读取根目录 `文件介绍.txt`。当前建议为：测试区域优先选择美国、欧洲或澳大利亚，避免下载全球 ERA5；ERA5 按层下载温度、气压、比湿和 geoheight/位势高度；用 2019 年做验证，训练使用探空气球资料，GNSS 仅用于验证；先选 2019 年 1、4、7、10 月检查四季效果。

### 15.2 新增资料接入与哈希复核

老师资料位于 `文件/`，包括：

- `config_v2.yaml`
- `IGSwhu_formatted.txt`
- `suominet_formatted.txt`
- `austrilian_stations_formatted.txt`
- `euref_formatted.txt`
- `ucar_cosmic_formatted.txt`

六个文件已复制到下载器默认路径：

```text
数据计算代码/download_lables/config_v2.yaml
数据计算代码/download_lables/GNSS_list/
```

2026-08-19 再次逐项 SHA256 比较，源文件与目标文件全部一致；未覆盖不一致文件，根目录 `文件/` 原始资料未改动。

### 15.3 离线站表审计结果

新增只读审计工具：

```text
数据计算代码/validate_station_lists_offline.py
```

审计报告：

```text
数据计算代码/validation_reports/station_list_audit_20260819.json
```

结果为：IGS、SuomiNet、澳大利亚、EUREF 四个站表通过生产解析器格式审计；UCAR 为条件性通过，包含 1 条生产解析器会跳过的非数值行、1 条经纬度范围异常/字段疑似错位记录，以及 9 个重复站点 ID。原始 UCAR 表未修改，暂未生成正式清洗替代表；重复 ID 的保留规则需依据源数据或老师口径确认。

注意：UCAR 的异常坐标记录在“能被 float 解析”意义下会进入现有生产解析器，因此不能只看脚本是否报错；必须使用离线审计结果和后续候选清洗表验收。

### 15.4 2019 四季下载前执行清单

新增文档：

```text
2019四季区域化验证_下载前执行清单_20260819.md
```

文档冻结了区域/源顺序、首次单日 GNSS 探针、ERA5 单日小样本、验收条件、四个月批处理前置条件和训练/超算启动条件。首个联网探针建议使用 EUREF、日期 `20190101`、单源、`--no_pwv`、隔离输出目录；不使用 SuomiNet，不首先批量下载 IGS，不下载全球 ERA5。

### 15.5 本地验证与剩余阻塞

已通过：

- 新增工具及现有下载相关 Python 文件 `py_compile`；
- 六个接入文件 SHA256 一致性复核；
- 站表离线审计：`PASS=4，CONDITIONAL=1，FAIL=0`；
- 数据管线离线预检：`BLOCKER=3，WARN=5`。

剩余 BLOCKER：

1. `boto3` 未安装，公共 S3 ERA5 下载器暂不可运行；
2. `cdsapi` 未安装，CDS ERA5 下载器暂不可运行；
3. CDS 脚本固定写入当前不可用的 `F:` 盘。

当前 WARN 仍包括 EGM96/`pygeodesy` 缺失、可选 EarthScope SDK 缺失、ERA5 S3 Windows 路径需参数化和认证策略告警。

### 15.6 网络、VPN、超算与模型生命周期边界

本轮未联网、未下载、未读取认证材料、未运行 `earthscope_auth_manual.py`，也未启动训练；当前不需要同济超算。真实 GNSS 或 ERA5 下载前必须先通知用户连接外网/VPN；若下载过慢、持续失败或触发限速，应停止而不是批量重试，并请用户检查 VPN。只有在探空气球训练数据、ERA5 积分链路和训练/验证切分冻结后，才请求连接同济超算。

本轮属于 `utility/coding，difficulty=2，risk=1，LOCAL`。本地 GPU 当前无模型驻留（已通过 `ollama ps` 检查）；本轮没有让本地小模型承担研究结论或架构决策，主结果由脚本、哈希和离线报告验证。

## 16. 当前计划与老师资料请求模板（2026-08-19）

新增两份可持续使用的文档：

```text
当前任务计划_20260819.md
给老师的资料请求完整模板_20260819.md
```

前者是当前阶段、已完成事项、下一步顺序、剩余阻塞和网络/安全边界的简要计划；后者包含正式版、微信简洁版、最小询问版，以及老师优先提供的文件清单。

当前最关键的老师资料是：探空气球训练数据、字段/单位说明、探空站坐标、训练/验证/测试划分要求，以及一个可读取的 ERA5 NetCDF 样例或变量维度说明。UCAR 异常记录和重复站点的清洗口径也需老师确认。

用户已说明 VPN 可用。后续真实 GNSS 探针开始前仍需确认外网连通；不使用消息中包含疑似密钥参数的订阅 URL，也不读取或传输任何账号、密码、Token 或认证材料。

## 17. 2019-01-01 EUREF 单日探针与命名兼容修复（2026-08-19）

### 17.1 首轮失败定位

在 VPN/外网可访问的前提下，首轮执行 EUREF、`20190101`、单源、`--no_pwv` 探针，服务器可访问但结果为 `0/15`。检查服务器目录发现 2019 年文件实际采用：

```text
{AC}0EPNE03_20190010000_01D_01H_TRO.TRO.gz
```

而旧代码只尝试 `EPNFIN` 命名。因此首轮失败定位为文件名规则不兼容，不是 VPN 或服务器不可达。

### 17.2 最小补丁与静态验证

修改：

```text
数据计算代码/gnss_downloader.py
数据计算代码/download_gnss_stations.py
```

补丁内容：

- `GNSSDownloader` 增加可配置 `save_dir`，默认仍为 `data/raw/validation/GNSS`；
- EUREF 分析中心下载仅按明确候选顺序尝试 `EPNFIN`、`EPNE03`，不进行目录批量扫描或无限重试；
- CLI 增加 `--raw_output_dir`，默认将原始下载隔离到 `<output_dir>/_raw_download`；
- `_done.json` 仍由站点级脚本管理，未改变其既有语义；
- 已通过 `py_compile`，并完成 AST 级参数、候选命名和输出目录检查。

### 17.3 修复后探针验收

输出目录：

```text
数据计算代码/data/raw/validation/GNSS_probe_20190101_euref_epne03_20260819/
```

验收报告：

```text
数据计算代码/validation_reports/euref_probe_audit_20260819.json
```

结果：

- 原始下载成功 `9/15` 个分析中心：GFZ、IGE、MUT、NKG、ROB、SGO、SUT、UPA、WUT；未成功的 6 个为 ASI、BEV、BKG、COD、LPT、RGA；
- 原始解析 `8883` 条记录、`371` 个站点；本地站表匹配后删除无坐标记录 `1462` 条，保留 `7421` 条有效记录；
- 输出 `310` 个站点 CSV，字段均为 `datetime,ztd_mm`；
- `308` 个站点有完整的 24 个小时记录；`PLND` 有 18 条，`ZECK` 有 11 条，需在正式四季统计中按缺测处理，不能补造数据；
- 无空文件、非数值 ZTD、重复时间或异常范围值；9 个 `.gz` 均可解压，无 `.part` 残留；
- 合并阶段 PowerShell 显示的 `NativeCommandError` 来自 `tqdm` 进度条输出管道，不代表最终合并失败；随后仅合并命令退出码为 0，并写出上述 310 个 CSV。

### 17.4 解释边界与后续动作

本探针证明 EUREF 2019 年文件可被当前下载器和解析器读取，但不证明 15 个分析中心全部可用，也不证明四季区域化验证或模型性能。下一步先固定该验收报告和缺测记录，再处理 ERA5 单日单区域分层变量小样本；暂不批量下载四个月、不下载全球 ERA5、不启动训练、不连接同济超算。
## 18. ERA5 单区域探针脚本准备（2026-08-19）

### 18.1 当前代码问题与最小修复

对 `数据计算代码/download_era5_hk_cds.py` 做了不触碰认证材料的静态准备：

- 默认输出从不可用的 `F:/era5_hk_cds` 改为工作区内 `data/raw/era5_targeted_cds`；
- 增加 `--output-dir`，允许显式指定工作区内输出目录；
- 增加 `--area N W S E`，可做美国、欧洲或澳大利亚的区域裁剪；
- 增加 `--days`，压力层数据可先限制到单日；
- 增加 `--dry-run`，dry-run 不读取 `ERA5下载key.txt`、不读取账号内容、不访问 CDS，只构造任务清单；
- 将 `cdsapi` 改为实际下载 worker 内延迟导入，使 `--help` 和 dry-run 在依赖未安装时仍可执行；
- 修正 CLI 帮助，使 `--raw_output_dir` 明确说明其为“原始下载与缓存目录”，而非纯 raw-only 目录。

### 18.2 无认证 dry-run 验证

已执行：

```text
python 数据计算代码/download_era5_hk_cds.py --years 2019 --months 1 --days 1 --groups pl --area 55 -10 35 30 --output-dir 数据计算代码/data/raw/era5_probe --dry-run
```

结果：

- 未读取认证文件；
- 生成 1 个压力层单日任务：`pl/20190101`；
- 区域为 `[55, -10, 35, 30]`，仅作为欧洲探针示例；
- 目标文件为 `data/raw/era5_probe/pl/2019/01/era5_pl_20190101.nc`；
- Python 语法检查通过。

### 18.3 仍然阻塞的事项

真实下载仍未启动，原因是当前环境缺少 `cdsapi`；公共 S3 路线仍缺少 `boto3`。本节没有读取 `ERA5下载key.txt`，没有使用任何账号、密码、Token、密钥或认证文件。下一步应先根据老师提供的训练/验证资料确定区域和变量最小集合，再由用户决定是否安装依赖并提供合规的 CDS 认证方式；在此之前不进行真实 ERA5 请求。